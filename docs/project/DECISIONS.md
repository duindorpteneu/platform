# Architecture and product decisions

| Date | Decision | Reason | Canon impact | Reversible |
|---|---|---|---|---|
| 2026-07-18 | Use project-local Supabase ports 54329 (API), 54339 (database), 54349 (Studio), 54359 (Mailpit) and 54369 (Analytics). | Port preflight showed these ports free and AGENTS.md requires isolation from sibling projects. | No canon change; isolates local services. | Yes |
| 2026-07-18 | Pin Supabase CLI 2.109.1 as a project-local devDependency. | Reproducible Docker image selection and migrations without global system changes. | No product impact. | Yes |
| 2026-07-18 | Give `authenticated` only table `SELECT` under RLS and route all application mutations through narrow RPCs. | Earlier tables had overly broad DML grants while later tables lacked usable read grants. | Enforces server validation, least privilege and auditability. | Yes, via migration. |
| 2026-07-18 | Keep the first dashboard on server-owned fixture data until staff Auth and RLS are connected. | The visual foundation can be validated without weakening the production authorization boundary. | Temporary implementation state; release remains blocked. | Yes |
| 2026-07-18 | Keep parent secrets in `private` schema and expose only narrow server-only RPC wrappers. | The private schema must not be accessed through the Data API. | Implements the prescribed secret boundary. | Yes |
| 2026-07-18 | Derive the versioned 256-bit QR bearer value with HMAC-SHA-256 from the per-environment random pepper and order/version, while storing only its SHA-256 hash. | This keeps the QR opaque and free of PII, permits rotation, and allows the same seasonal QR to be rendered again without storing plaintext. | Implements the canon's 256-bit bearer, hash-only storage and `PARENT_TOKEN_PEPPER` contract. | Yes, before production issuance. |
| 2026-07-18 | Require the bearer token again on fulfilment commit instead of trusting a prior lookup result. | A scan result can become stale through rotation, refund or concurrent use. | Enforces the required live token recheck inside the transaction. | Yes. |
| 2026-07-18 | Enforce staff AAL2 in middleware, server context and the database role helper. | A UI-only MFA check would leave RPC and stale-session bypasses possible. The database helper raises `42501` when AAL2 or an active profile is missing. | Implements mandatory staff MFA and immediate blocking of inactive profiles. | Yes, via migration. |
| 2026-07-18 | Expose only `public`, `graphql_public` and `app` through the local Data API; keep `private` unexposed. | User-scoped server clients invoke guarded `app` RPCs, while parent secrets remain behind narrow public service-role RPC wrappers. | Preserves the canon's server/client and secret boundary. | Yes. |
| 2026-07-18 | Serve the backoffice dashboard through one AAL2- and role-guarded aggregate RPC with a strict Zod response contract. | Metrics, recent orders and audit activity must share one consistent database snapshot and must fail closed on unexpected data. The response omits member e-mail and audit metadata. | Replaces the temporary visual fixture without widening access or exposing unnecessary personal data. | Yes, via a forward migration and contract version. |
| 2026-07-18 | Keep active-season shell context in a separate minimal staff RPC. | `uitgifte` needs the current season in the shared shell but must not receive backoffice metrics, members or audit activity. | Preserves least privilege across the three staff surfaces. | Yes. |
| 2026-07-18 | Use self-cleaning fictitious browser fixtures only inside the dashboard regression script. | Production-like states are needed for screenshot and responsive validation, while persistent sample users or records could be mistaken for operational data. | No product data is seeded; test accounts, MFA factors and records are removed in `finally`. | Yes. |
| 2026-07-18 | Split the member overview into a PII-minimal list RPC and an explicit member-detail RPC. | Searchable tables do not need parent e-mail or linkage data; details do. Separate strict contracts reduce accidental bulk disclosure and keep `uitgifte` excluded from both RPCs. | Implements least privilege while preserving the canonieke right-hand detail panel. | Yes, via forward migration and contract version. |
| 2026-07-18 | Execute all member filtering and pagination inside the AAL2-guarded database RPC. | Client-side filtering would require overfetching and could produce inconsistent totals. Server-side filters cover name, relation number, team, payment, order, article, size and fulfilment. | Implements the canonieke operational filters without widening browser trust. | Yes. |
| 2026-07-18 | Convert validated Sportlink domain rows to an explicit snake_case database payload before RPC calls. | TypeScript domain naming and PostgreSQL record keys differ; an explicit adapter makes the boundary testable and prevents nullable record fields at runtime. | No product behavior change; completes reliable preview and transactional commit. | Yes. |
| 2026-07-18 | Store a size snapshot on every order line and make the size of a used variant immutable. | Historical orders and fulfilments must not change when the active catalog evolves. | Implements canonical season history without hard-deleting used variants. | Yes, via forward migration. |
| 2026-07-20 | Store staff-entered clothing sizes per member, season and article as a preparatory profile; keep active order lines authoritative. | Children grow between seasons and a previously submitted size may predate an order. Season scoping prevents silent rollover, while order-line snapshots remain the financial, inventory and fulfilment truth. | Adds the requested staff workflow without changing canonical order or issue semantics. | Yes, via forward migration. |
| 2026-07-20 | Model association address and optional pickup address separately, while deriving the legacy pickup-location string for e-mail shortcodes. | Duindorp SV remains the fixed association, but physical collection may take place at another named address. | Clarifies operational location data without adding club branding or changing template shortcodes. | Yes, via forward migration. |
| 2026-07-18 | Standalone QR revocation leaves zero active tokens; an explicit later rotation creates the next version. | Revocation must immediately block scanning, while reactivation must remain deliberate, reasoned and auditable. Resend never rotates. | Resolves the canon ambiguity with the safest least-privilege behavior. | Yes, before production issuance. |
| 2026-07-18 | Treat fulfilment correction as a whole-order-line administrative reversal. `ready_for_pickup` restores the fulfilled reservation; `backorder` releases it. | The canon does not define partial correction within a quantity greater than one or a separate returns workflow. | Keeps the MVP atomic and inventory-consistent without inventing returns/exchanges. | Yes, with a later canonical partial-quantity model. |
| 2026-07-18 | Keep an all-cancelled paid order at the existing derived status `Afgerond`. | The canon defines no additional status for this edge case and forbids invented order states. | Preserves the exact canonical order-status vocabulary. | Yes, via a documented future canon revision. |
| 2026-07-18 | Accept QR hashes and trusted payment mutations only through service-role RPCs after an AAL2/RBAC route guard, and grant service_role only schema USAGE plus explicit function EXECUTE. | Browser-callable SECURITY DEFINER functions could otherwise bypass route validation or choose secret material. | Strengthens the prescribed server/client secret boundary without granting service_role table access through the app. | Yes, via forward migration. |
| 2026-07-18 | Keep the OTP code in a fixed, non-editable SendGrid block outside the durable e-mail queue. | The canon requires a verification template but its closed shortcode list has no OTP shortcode. Persisting plaintext OTP in a job would violate the hash-only secret boundary. | Safest resolution of the canon ambiguity: six-digit OTP remains direct, single-purpose and never durable; surrounding verification content cannot be edited. | Yes, after an explicit canon shortcode decision. |
| 2026-07-18 | Store editable e-mail sources as validated plain text and render escaped HTML server-side. | This supports accessible multipart mail while excluding scripts, arbitrary expressions, forms and unsanitized HTML. | Implements the closed shortcode allowlist without adding a free-form template language. | Yes. |
| 2026-07-18 | Bind bulk confirmation to a ten-minute HMAC preview token and immutable per-job template snapshots. | Confirmation must preserve the reviewed template, exact unique order selection and batch identity even when a template is edited later. | Implements fictional previews, batches up to 2,000 orders and replay-safe per-order jobs. | Yes. |
| 2026-07-18 | Do not automatically reclaim e-mail jobs left in `processing`. | SendGrid Mail Send has no end-to-end idempotency guarantee; automatic reclaim after provider acceptance but before local completion could send twice. | Fails operationally closed: such jobs require manual investigation rather than risking duplicate member mail. | Yes, if a provider reconciliation mechanism is introduced. |
| 2026-07-18 | Use the classic Mollie form webhook and always fetch the current payment object server-side. | The webhook body contains only an identifier and is not trusted as payment evidence; redirect pages never mutate payment state. | Implements the canon's webhook-first Payments API boundary without adopting beta or Orders APIs. | Yes. |
| 2026-07-18 | Treat any observed refund amount as fail-closed `refunded`, revoke active QR access, and preserve fulfilment history. | Partial refund behavior is outside the MVP and must not leave issue rights active while financial state is ambiguous. | Applies the strictest safe interpretation; refund initiation remains outside scope. | Yes, after a future partial-refund canon. |
| 2026-07-18 | Derive the parent-visible effective payment state by prioritizing `paid`, then `refunded`, then `duplicate_paid`, then the latest attempt. | A later duplicate attempt must not hide the primary paid state or its active QR; refund of the primary payment must still block it. | Keeps multi-attempt provider history compatible with the one-order payment truth. | Yes, via forward migration. |
| 2026-07-18 | Keep club name and logo immutable as `Duindorp SV` and the supplied original logo; settings may change only operational contact, pickup, season, amounts, staff and provider switches. | The higher-priority design canon forbids generic branding and logo reinterpretation. | Resolves the lower-level settings wording without introducing multi-tenant or mutable branding. | Only after an explicit canon revision. |
| 2026-07-18 | Require both the environment flag and the beheerder-controlled database switch before Mollie or SendGrid can create outgoing provider traffic. | Runtime configuration must remain a hard emergency ceiling, while operations needs an audited immediate pause control. | Adds no new feature flag; it implements two independent safety layers for the two canonical providers. | Yes. |
| 2026-07-18 | Protect browser mutations with exact same-origin validation plus Fetch Metadata and the `X-Duindorp-CSRF: same-origin` proof header. | HttpOnly cookies alone do not prove same-origin intent; the custom non-simple header and origin/host checks make cross-site form and fetch submissions fail closed. | Strengthens the required server-side authorization boundary without adding client trust. | Yes. |
| 2026-07-18 | Generate XLSX exports server-side with pinned ExcelJS and neutralize spreadsheet formulas in every text cell for CSV and XLSX. | The MVP requires native Excel output, while member-controlled fields must not become spreadsheet commands. | Implements the canonical export formats within the modular monolith. | Yes. |
| 2026-07-18 | Use prefixed repository Actions configuration and two repository-level self-hosted runners until protected GitHub Environments are available. | The private repository remains on GitHub Free, while staging and production must deploy to an isolated VPS without touching Castivo. Separate users, labels, paths and services reduce cross-project impact; a release-author/dispatcher split adds a temporary four-eyes gate. | Deployment remains canon-compatible but carries a documented weaker workflow-administration boundary until a plan upgrade. | Yes; migrate the same unprefixed contract to protected Environments after upgrade. |
| 2026-07-18 | Supersede the fixed OTP provider block with the editable `verification_code` backoffice template and dedicated `{{verificatiecode}}` shortcode. | The product owner explicitly clarified that the ouder-OTP template must be managed, previewed, versioned and audited like the other five messages. The real six-digit value is still rendered only in memory during direct delivery and never enters the durable queue or audit metadata. | Resolves the earlier recorded canon ambiguity without weakening hash-only OTP storage or adding a seventh template. | Yes, through a later product decision and forward migration. |
| 2026-07-19 | Supersede the repository-level systemd deploymodel with protected GitHub environments, one Rootless-Docker image per SHA and digest-identical staging-to-production promotion. | The VPS, runners and environments are now available and the release owner explicitly fixed routes, ports, paths and approval flow. Runtime injection of public Supabase browserconfig keeps one image environment-neutral. | No product-scope change; `/admin` is a protected operational alias for canonical `/backoffice`. | Yes, via a new reviewed deployment decision. |
| 2026-07-19 | Define immutable image identity as the verified tuple of OCI manifest digest, image config digest and transported archive digest. | Docker's classic and containerd image stores expose different valid values through `.Id` after loading the same `docker save` archive. Verifying all three byte-level identities avoids a false mismatch without weakening promotion integrity. | No product change; staging and production still receive the exact same artifact from one build. | Yes, through a versioned release-manifest schema. |
| 2026-07-19 | Manage the hosted PostgREST exposed-schema allowlist with a forward migration on the `authenticator` role. | Local Supabase already exposes `public`, `graphql_public` and `app`, but the hosted project returned `PGRST106` because `app` was absent from its Data API configuration. Keeping the exact allowlist in versioned SQL prevents dashboard drift; `private` remains excluded. | No product change; it makes the existing guarded `app` RPC boundary reachable in hosted environments. | Yes, through a later forward migration. |
| 2026-07-19 | Treat blank optional provider environment values as absent and omit them from generated runtime files. | GitHub passes unset optional environment variables as empty strings, while typed runtime validation correctly rejects a present but invalid e-mail address. Disabled providers must not make unrelated health and public routes fail. Enabled providers still require every configured value. | No product change; provider safety remains fail-closed when enabled. | Yes. |
| 2026-07-19 | Correlate signed SendGrid operational events through the existing `email_job_id` custom argument and retain provider IDs only as observations. | SendGrid documents that Mail Send `X-Message-ID` and Event Webhook `sg_message_id` are different, and asynchronous bounce events may omit the latter. The UUID contains no recipient data and is already generated locally. | Preserves idempotent delivery tracking without adding PII to provider metadata. | Yes, through a later forward migration. |
| 2026-07-19 | Use a staging-only GitHub Actions scheduler as acceptance infrastructure, not as the production scheduler. | It can securely consume the staging `CRON_SECRET` and exercise real queue/retention routes now, but GitHub schedule latency cannot guarantee a production one-minute SLA. | No product scope change; production remains gated on a monitored scheduler. | Yes. |
| 2026-07-19 | Configure SendGrid webhook settings and signing by immutable webhook ID during a staging-only provider smoke, then store only the public P-256 verification key as environment configuration. | Mail Send acceptance alone does not prove webhook URL, event selection or signing. The ID differentiates staging from production while the public key lets the app verify exact raw-body signatures. | Completes the canonical signed Event Webhook boundary without exposing private key material or activating production. | Yes. |
| 2026-07-20 | Accept the native Sportlink members export aliases and apply explicit previewed fallbacks when it omits season status, roepnaam or local team. | The operational export uses `Rel. code`, `Roepnaam` and `Lokale teams`, omits a season-status column, and may leave a roepnaam or team blank. Rejecting all rows makes the canonical CSV route unusable; silently inventing a real team would be misleading. | Rows without a status are imported as active after preview confirmation, blank teams become `Niet ingedeeld`, and a blank roepnaam uses Sportlink initials. Every fallback and count is shown in preview and stored with the batch mapping. | Yes, by exporting an explicit season-status field and complete team/roepnaam data. |
| 2026-07-20 | Treat member inactivation as a reversible current-season eligibility switch, never as deletion or historical cancellation. | Staff must be able to remove a member from current operations without changing orders, payments, fulfilments or parent history. | New order assignment and new manual/Mollie payment initiation fail closed while inactive or when the order is not in the current open season; already-started provider reconciliation and existing records remain intact. Every transition requires a reason and audit entry. | Yes, via a later season-membership model if the canon requires per-season records. |
| 2026-07-20 | Allow bulk article links to change only for open seasons and preserve all historical order lines. | Season preparation needs efficient reversible catalog management, while archived history must remain immutable. | Bulk link/unlink is role-guarded and audited; article and order records are never deleted or rewritten. | Yes. |
| 2026-08-02 | Laat uitsluitend een beheerder met AAL2 pakketprijs, standaard en revisies beheren; publiceer altijd een immutable revisie met server-afgeleide productsnapshots en een inhoudshash. | Pakketprijs/default zijn commerciële bevoegdheden en gelijktijdige browserwrites mogen geen gereviewde inhoud vervangen. | De gebruiker maakt zelf producten, maten en pakketten; er worden geen Speler-/Keeperfixtures aangemaakt. Catalogusrelaties en de laatste actieve maat zijn beschermd zolang een concept of actief pakket ze nodig heeft. | Ja, via een nieuwe forward revisie; gepubliceerde historie blijft immutable. |
| 2026-07-20 | Add team articles with additive, snapshot-bound preview semantics and require one explicit variant/size per selected article. | The product owner requested team assignment, while the data model has no independent per-member size profile and existing orders may already contain a different size. | Eligible active members receive only missing articles. New orders use the active season default amount; existing unpaid orders keep their exact amount and lines. Paid orders, inactive members and already assigned articles are skipped and reported. Commit requires a ten-minute HMAC token and the same season/team/order/payment snapshot. | Yes, if a canonical per-member size profile is introduced. |
| 2026-07-20 | Treat bulk team activation as snapshot-bound current-season eligibility changes inside one audited transaction. | A team operation must not delete orders, payments or fulfilment history and must remain traceable per changed member. | Preview reports changed/unchanged counts; commit requires a reason, a signed matching snapshot and writes both per-member and aggregate audit events under AAL2/RBAC. Relevant member DML is serialized during the final check. | Yes. |
| 2026-07-20 | Gate every deployment on a service-only, data-free PostgREST contract version after migrations and before application activation. | A successful SQL migration does not by itself prove that hosted PostgREST has reloaded newly introduced RPCs. A normal service-role OpenAPI view cannot inspect functions granted only to `authenticated`. | The probe exposes no business data, mutations or staff identity; it verifies the cached contract version and database privileges without weakening the AAL2 guards on operational RPCs. | Yes, by replacing the versioned probe in a later forward migration. |
| 2026-07-21 | Run the one-minute scheduler as a hardened service in the environment-specific Compose project and require an independent external dead-man-switch for production. | GitHub schedules cannot guarantee a one-minute SLA; a same-VPS scheduler alone cannot detect VPS, Caddy or network loss. | E-mail, retention and internal health run without GitHub timing dependency. Production fails runtime preflight without `OPERATIONS_HEARTBEAT_URL`; the external monitor alerts when healthy pings stop. | Yes, when a managed scheduler/monitoring platform replaces this service. |
| 2026-07-21 | Split automatic staging deployment from explicit production promotion. | A waiting productionapproval held the global workflow concurrency lock and blocked newer staging fixes. | Every main SHA can reach staging independently; production downloads the exact successful staging artifacts by run ID and still requires the protected environmentapproval. | Yes. |
| 2026-07-21 | Classify ambiguous SendGrid transport outcomes as `delivery_uncertain` and permit recovery only from signed provider events or an AAL2 administrator's documented provider evidence. | SendGrid has no end-to-end idempotency key; retrying after an accepted response was lost can send duplicate member mail. | Timeout, network, 408/5xx and missing provider-ID never auto-resend. Proven non-acceptance can be retried; proven acceptance is marked sent without sending again, with optimistic concurrency and append-only audit. | Yes, if the provider adds a safe reconciliation/idempotency API. |
| 2026-07-21 | Verify the post-MFA Supabase access-token locally against the project's asymmetric JWKS before issuing an opaque, server-managed application session. | Both VPS-forwarded and browser-direct hosted PostgREST requests with a valid user JWT stalled, while the public ES256 JWKS and service-role Data API routes were healthy. Local signature and claim verification removes that transport dependency without trusting an unverified browser claim. | The app accepts only the configured issuer, audience `authenticated`, algorithm ES256, valid lifetime, role `authenticated` and `aal2`; PostgreSQL then resolves the active profile and role. The session is 256-bit, hash-only, eight hours valid, Secure/HttpOnly/SameSite, revalidated per request and revoked on application logout. The temporary authenticated exchange privilege is withdrawn in the next forward migration. | Yes, when the hosted user-JWT path is operationally reliable. |
| 2026-07-21 | Inject each environment's public ES256 JWKS as validated runtime configuration. | The staging container's outbound JWKS retrieval stalled only for valid tokens, despite the endpoint being publicly healthy elsewhere. The image must remain identical across staging and production and no signing secret may be added. | Deploy preflight requires an EC/P-256/ES256 keyset and writes it to the protected runtime env file. JWT verification uses a local JWK set with no request-time network call; key rotation requires updating the public GitHub environment variable before deployment. | Yes, when reliable container egress is established. |
| 2026-08-02 | Adopt the product-owner-approved package, member-season, selective-access, dynamic-import, DOB and scanner-PWA model in `MVP_CANON_ADDENDUM_v1.1.md`. | The verified v1.0 implementation models loose order lines and global member-season state and cannot safely express package snapshots, protected imported sizes, multi-season grants or allocation-gated QR use. | Explicitly supersedes only the named v1.0 rules. DOB is visible only to beheerder and the granted parent; supplier planning remains aggregate-only; physical Free-Kick issuance uses a separate `uitgifte` grant. No product/package/catalog fixtures are seeded. | Forward-only during the Phase B compatibility window. |
| 2026-08-02 | Keep Free-Kick planning aggregate-only and expose first name plus gender only through a separately authorized valid issuance scan. | This satisfies the product owner's minimum-data instruction without weakening the earlier binding prohibition on individual supplier planning data. | Supplier is a separate principal, not a fourth staff role. The scanner RPC cannot return DOB, surname, e-mail, team, relation number or individual payment details. | Yes, by narrowing the issuance display further. |
| 2026-08-02 | Use no temporary reservation for unpaid demand. | The approved safe default is operationally simpler and cannot accidentally promise stock to an unpaid package. | Unpaid demand remains in planning totals; availability communication is explicitly non-guaranteed and QR remains inactive. | Yes, through a later explicit product decision. |
| 2026-08-02 | Treat the existing pin path as legacy and keep it disabled behind a dedicated feature flag while cash becomes beheerder-only. | Existing behavior must not disappear silently, but Phase B makes Mollie public and cash administrative. | No pin deletion or automatic migration; all existing records remain historical. | Yes. |
| 2026-08-02 | Clean staging operational data only with a dry-run-first allowlist command that proves the staging identity and preserves every staff/Auth account. | The product owner requested an empty staging member/catalog workspace while retaining the administrator account. A generic reset or production-capable fixture RPC is too broad. | The wipe is an explicit staging operation outside forward product migrations, with backup, counts, reconciliation and audit evidence. | Data deletion is recoverable from the required pre-wipe backup. |
| 2026-08-03 | Separate parent authentication and QR cryptography and use a two-key versioned QR keyring with a random nonce per locator. | A deterministic locator tied to `PARENT_TOKEN_PEPPER` made unrelated session rotation break QR identities and offered no safe overlap window. | `QR_TOKEN_PEPPER` derives only versioned locators/grants; the database stores nonce, version, fingerprint and hashes. Current plus one distinct previous key are accepted during rotation, and retirement blocks until health reports no previous-key locator or open grant. | Yes, by audited locator rotation and removal of the previous key after the zero gate. |
| 2026-08-03 | Limit scanner-PWA access to `beheerder` and `uitgifte`; keep `kledingcommissie` in the browser backoffice for operational correction. | Physical scanning is a separate least-privilege duty and the approved Phase B role proposal does not require every clothing administrator to enter the scanner. | Middleware, navigation, exchange/commit RPCs and staging role acceptance enforce the same boundary. | Yes, by separately assigning the `uitgifte` role when Free-Kick physically hands out packages. |
| 2026-08-02 | Upload Sportlink CSV as the bounded request body with an encoded filename header instead of parsing multipart before enforcing the cap. | The platform `formData()` consumer buffered an attacker-controlled chunked body before the nominal size check. The import needs exactly one file and no ancillary form fields. | Preserves extension/MIME/filename checks while making the actual byte stream authoritative; the later dynamic mapping workflow can reuse the same upload boundary. | Yes, by adopting a pinned streaming multipart parser with equivalent total/file/part/header limits. |
| 2026-08-02 | Keep Mollie acceptance fixtures outside the product API and product-owned durable schema. | A service-role fixture RPC remains production-reachable even when its intended caller is staging. A durable fixture ledger also creates unsafe deploy/acceptance overlap around global setting restoration. | Forward-drop every acceptance-only RPC/table. The external runner is pinned to the exact staging host/project/database/TLS contract, requires an already open active season and enabled test configuration, never changes global settings and deletes only run-marked `example.invalid` data. | No; future acceptance helpers must remain external and staging-target-locked. |
| 2026-08-02 | Maak oudertoegang een expliciete beheerdergrant per lid-seizoen en migreer legacy links via een geblokkeerde eenrichtings cutover. | Import mag geen toegang verlenen, gedeelde adressen mogen niet automatisch alle kinderen ontsluiten en een direct schema-omslagpunt zou bestaande ouders buitensluiten. | AAL2-beheerpreflight, expliciete selectie, één account/uitnodiging per genormaliseerd adres, actieve-grantautorisatie en negatieve seizoen-/IDOR-tests. De compatibiliteitsflag blijft uit totdat alle reviewgrants zijn opgelost. | De cutover zelf is bewust eenrichtings; vóór activering blijft het legacy-pad beschikbaar. |
| 2026-08-02 | Verzend de portaaluitnodiging zonder login-, OTP- of magic-linktoken en maak OTP pas na een eigen loginpoging. | Een uitnodigingsmail is geen authenticatiebewijs en mag geen herbruikbaar geheim introduceren. | De duurzame job bevat alleen accountcontext, clubcontact en de portaalroute; grants, kindgegevens en ontvangeradres worden niet in het beheer-/auditcontract teruggegeven. | Ja, door een volgende gepubliceerde templateversie binnen hetzelfde veilige tokencontract. |
| 2026-08-02 | Match geïmporteerde maten uitsluitend op productgebonden label, leverancierscode of expliciete alias na NFKC-, whitespace- en case-normalisatie. | Fuzzy matching kan een verkeerde voorraadvariant reserveren; `Anders…` is volgens het addendum een conflict en nooit een SKU. | Productbeheer beheert maximaal 25 aliassen per variant. Genormaliseerde collisions worden geblokkeerd of als bestaand catalogusconflict getoond; oude en nieuwe mutatie-RPC's delen één productslot. | Ja, aliassen kunnen geaudit worden aangepast zolang historische variantidentiteiten intact blijven. |
| 2026-08-02 | Stage ruwe dynamische imports uitsluitend kortlevend met een aparte omgevingsunieke AES-256-GCM-rootkey en twee onafhankelijke featurepoorten. | De beheerder moet na upload kunnen mappen en dry-runnen zonder dat niet-geselecteerde CSV-kolommen plaintext of duurzaam worden opgeslagen. Bestaande peppers en frameworkkeys hebben een ander rotatie- en dreigingsdomein. | De app leidt per batch via HKDF een sleutel af, bindt ciphertext met AAD aan batch/checksum, bewaart alleen ciphertext maximaal 1–72 uur en laat cleanup/health ook bij een uitgeschakelde import doorlopen. Runtime én DB moeten actief zijn. | Ja; keyrotatie uitsluitend met nul actieve raw uploads of gecontroleerde purge. |
| 2026-08-02 | Bewaar dynamische importmapping als immutable revisies met alleen geselecteerde headerdigests en herbruikbare presets met genormaliseerde headernamen. | Niet-geselecteerde kolommen mogen niet duurzaam blijven staan en een oude preset of catalogus mag nooit stil als actuele waarheid worden gebruikt. | Iedere mapping bindt batch, actor, seizoen, uploadheaderhash, policyhash en actuele product-/variant-/aliashash; presets bevatten geen bronwaarden en worden per upload opnieuw gecontroleerd. Maatmatching blijft exact en productgebonden. | Ja, door een nieuwe mappingrevisie; historische revisies blijven immutable. |
| 2026-08-03 | Behandel ontbrekend relatienummer/e-mailadres als echte ontbrekende identiteit en nooit als een gegenereerde placeholder. | Een placeholder kan twee leden ten onrechte samenvoegen; een syntactisch ongeldig geïmporteerd ouderadres moet zichtbaar kunnen blijven voor preflight en herstel. | Optionele waarden worden canoniek getrimd, maar geldigheid wordt procesmatig beoordeeld. Een bestaande primaire Sportlink-ID wijzigt alleen via een expliciete beheerworkflow; DOB wordt nooit de enige matchanker. | Ja, via een geaudite identity-reconciliationworkflow; historische externe identiteiten blijven behouden. |
| 2026-08-03 | Gebruik één PII-vrije action-itemledger met door de database gealloceerde dedupe-episodes en rolgebonden zichtbaarheid. | Voorraadtekorten, maatconflicten en betaal-/queueproblemen moeten herstelbaar en auditbaar zijn zonder persoonsgegevens te dupliceren in brede operationele context. | Normale rollen kunnen items niet direct schrijven; domeintransacties gebruiken een SHA-256-conditiedigest en een gesloten contextcontract. Eén conditionele unieke index plus transactieslot staat maximaal één actieve episode toe. Oplossen of afwijzen vereist AAL2, bevoegdheid en een reden; records worden niet destructief verwijderd. | Ja, typen en veilige context kunnen forward-only worden uitgebreid. |
| 2026-08-03 | Behandel een gereserveerde pakketmaat als logistiek immutable en modelleer iedere latere ouderkeuze als een afzonderlijk wijzigingsverzoek. | Een stille update zou fysieke voorraad, historische maatsnapshot en uitgifte door elkaar halen; retries of races mogen geen nieuwe SLA-episode of dubbele orderregel maken. | Een semantisch identieke retry behoudt request-ID, tijd, actiepunt en historie. Goedkeuring geeft de oude reservering vrij en maakt een nieuwe backorderregel; intrekken kan alleen tegen exact dezelfde nog gereserveerde regel. Uitgegeven regels blijven vergrendeld. | Ja, via een nieuwe geaudite beheerworkflow; historische regels en verzoeken blijven immutable. |
| 2026-08-03 | Scheid betaalsettlement volledig van QR-readiness en bind iedere nieuwe betaling aan lid-seizoen, pakketsnapshot en metadataversie. | Betaling bewijst geldontvangst, maar zonder geldige harde allocatie bestaat nog geen veilig afhaalrecht. De oude kas- en Molliepaden activeerden een scanbaar bearer-token te vroeg en Mollie v1/v2-contractdrift maakte checkout onmogelijk. | Kas is beheerder+AAL2, cash-only zolang `legacy_card_payment` uitstaat, reden- en requestgebonden. Nieuwe Molliepogingen gebruiken v2; bestaande v1-pogingen blijven exact op hun vastgelegde versie reconcileerbaar. Geen van beide maakt een QR; de allocator wordt de enige creator. | Ja; historische betalingen/QR's blijven leesbaar, oude service-execute-rechten zijn ingetrokken en cutover blijft feature-gated. |
| 2026-08-03 | Modelleer fysieke voorraad als een append-only vectorjournaal en harde reserveringen als een afzonderlijke seizoensgebonden allocatieprojectie. | Ontvangst, reservering, uitgifte en betaling zijn onafhankelijke assen; de legacy ontvangstregels bevatten geen seizoen en mogen bij backfill niet aan het actieve seizoen worden toegeschreven. | Conceptleveringen hebben een volledige bevestigde maatmatrix en wijzigen niets vóór atomair posten. Alleen betaald + maatgeldig wordt FIFO gealloceerd. Legacy gereserveerd/uitgegeven blijft lossless, vrije voorraad vereist expliciete AAL2-seizoentoewijzing en iedere afwijking houdt de cutoverflag uit. | Ja, via nieuwe journal-/allocatie-events; reeds vastgelegde historie blijft immutable. |
| 2026-08-03 | Activeer mail-v2 pas wanneer iedere catalogussleutel naast gepubliceerde inhoud ook een forward-only geregistreerde producent heeft. | Een template is geen operationeel proces; selectie, vlak-voor-enqueue-hercontrole, consolidatie, suppressie, idempotentie en definitieve foutafhandeling moeten afzonderlijk bewezen zijn. | De capabilityledger is private/default-deny en onderdeel van de optimistic cutoverhash. Redenen voor cutover, pauze en herstel worden alleen als SHA-256-digest plus lengte geaudit. Een mislukte fulfilmentprojectie blijft herstelbaar zonder bronevent of mislukte job te wissen. | Ja voor pauzeren en nieuwe producentversies; het eerste cutoverwatermerk blijft immutable. |
| 2026-08-03 | Bind iedere SendGrid-verzendpoging aan een eigen immutable attempt-ID en behandel HTTP-acceptatie en signed provider-events als afzonderlijke bewijzen. | SendGrid garandeert niet dat de Mail Send-header dezelfde identifier gebruikt als het Event Webhook; retries en events kunnen bovendien in tegengestelde volgorde arriveren. Afleiden uit de mutabele jobstatus kan dan een dubbele mail of verkeerde herstelactie veroorzaken. | De niet-persoonlijke attempt-ID gaat mee als `custom_args`; provider-events worden exact aan job plus attempt gebonden, afwijkende identiteiten gaan PII-vrij in quarantaine en alleen een AAL2-beheerder kan een onzekere poging met extern bewijs herstellen. Legacy ambiguïteit en quarantaine blokkeren release-readiness. | Ja voor een volgende RPC-versie; het attempt-, outcome- en eventjournaal blijft immutable. |
| 2026-08-03 | Laat herinneringsregels alleen bestaande, bewezen mail-v2-doelgroepen plannen en maak iedere nieuwe of gewijzigde regel standaard inactief. | Een scheduler mag geen tweede segmentatiewaarheid introduceren en een configuratiewijziging mag nooit stil live mail activeren. | Beheerder+AAL2 configureert en activeert met reden; de worker hergebruikt vlak vóór enqueue de canonieke campagnekandidaten, episodes, consolidatie en suppressie. Stille uren zijn vast `Europe/Amsterdam`; iedere run/revisie is immutable en plannerfouten blokkeren readiness. | Ja via een nieuwe geaudite revisie; bestaande run- en eventhistorie blijft immutable. |
| 2026-08-03 | Houd LOGIN_OTP buiten de duurzame e-mailqueue en render/verzend uitsluitend na de neutrale HTTP-respons vanuit een aparte attempt-gebonden ledger. | Een queuejob, rendersnapshot of renderhash zou de zes cijfers duurzaam of brute-forcebaar bewaren; wachten op SendGrid maakt bekende adressen bovendien meetbaar trager. | Alleen de challenge bewaart de bestaande secret-hash. De provider ontvangt `delivery_kind` plus een random poging-ID zonder ontvanger/code; timeout/5xx/ontbrekende provider-ID wordt `delivery_uncertain` en nooit automatisch opnieuw verzonden. Legacy blijft uitsluitend vóór het immutable mail-v2-watermerk beschikbaar. | Ja via een volgende attempt-/RPC-versie; het eerste outcome- en providerjournaal blijft immutable. |
| 2026-08-03 | Geef Free-Kick een afzonderlijke, read-only leverancierprincipal met expliciete open-seizoengrants en een eigen hash-only sessie. | Een vierde personeelsrol of hergebruik van de voorraadworkspace zou individuele wachtlijst-, order- en betaalgegevens ontsluiten. | Een eenmalige 256-bit toegangssleutel wordt alleen gehasht bewaard; rotatie, intrekken en grantwijziging trekken alle sessies in. Het contract scheidt voorraad per productmaat van open vraag per geslacht en geeft onbekende maten uitsluitend als telling terug. | Ja, door de sleutel te roteren of de principal niet-destructief uit te schakelen; eventhistorie blijft immutable. |
| 2026-08-03 | Maak de geautoriseerde stagingreset een target-locked externe operatie met een vaste 90-tabellenallowlist en behoud 27 staff-/configuratie-/audit-/operations-tabellen plus heel `auth`. | Een migration, `CASCADE` of generieke “alles behalve”-query kan production, beheerders, MFA, branding of releasebewijs raken. De gewone restore-drill bewaart bovendien geen herstelpunt. | Apply pauzeert alleen het exacte staging-Composeproject, vereist uitgeschakelde providers, maakt en netwerkloos herstelt eerst een AES-256-encrypted dump, vergelijkt de datadigest opnieuw onder `ACCESS EXCLUSIVE`-locks en bewijst alle preserve-invarianten na commit. | De wisactie zelf is alleen via de encrypted back-up omkeerbaar; de tooling kan veilig opnieuw worden uitgevoerd op een lege stagingdatabase. |
| 2026-08-03 | Supersedeer de cleanupinventaris naar exact 95 wistabellen en 28 bewaartabellen plus heel `auth`. | Nieuwe Phase-B-tabellen moeten expliciet als operationeel of duurzaam worden geclassificeerd; het oude 90/27-besluit is niet langer schema-compleet. | Contract, preflight, apply en tests eisen samen exact 123 bekende `app`/`private`-tabellen en blokkeren iedere drift. | Ja, uitsluitend met een nieuw gereviewd inventarisbesluit en bijbehorende tests. |
| 2026-08-07 | Supersedeer de cleanupinventaris naar exact 100 wistabellen en 28 bewaartabellen plus heel `auth`, en voer ook dry-run uitsluitend op de geïsoleerde stagingrunner uit. | De delivery-notificatie-, package-change-, payment-reconcile- en SendGrid-testacceptatietabellen zijn operationele ledgers die bij de gevraagde stagingreset moeten worden gewist. De cleanupentrypoint handhaaft altijd de runnerboundary en kan daarom niet op `ubuntu-latest` draaien. | Contract, preflight, bewijs en tests eisen samen exact 128 bekende `app`/`private`-tabellen. Auth, personeel, instellingen, templates, branding, seizoenen en audit blijven behouden; elke toekomstige schemadrift blokkeert vóór mutatie. | Ja, uitsluitend via een opnieuw gereviewd expliciet inventarisbesluit en een verse herstelgeteste encrypted backup. |
| 2026-08-10 | Scheid medewerkeruitnodiging en wachtwoordherstel in twee exacte Supabase Auth-callbackroutes en vereis bestaande TOTP vóór herstel wanneer die factor aanwezig is. | De uitnodigingsparser wees `type=recovery` bewust af en een dashboardreset viel terug op de algemene Site URL. Bij een bestaand MFA-account weigert Supabase bovendien terecht een AAL1-wachtwoordwijziging. | De app biedt een neutraal en rate-limited herstelverzoek, valideert en wist het hashfragment vóór browserclientinitialisatie, vraagt bestaande TOTP, trekt alle app-/scannersessies in, beëindigt Supabase-sessies en vereist daarna een verse login. | Ja; callbackpaden kunnen alleen forward-only worden vervangen met gelijkwaardige anti-enumeratie, MFA, sessie-intrekking en audit. |
| 2026-08-03 | Bind self-hosted staging en production aan aparte vaste runnernamen, Unix-users, private homes, Rootless-Docker-sockets/data-roots en 0700-runtimebomen. | Labels, poorten en Composeprojectnamen vormen geen securityboundary wanneer beide jobs dezelfde `deploy`-principal gebruiken. | Alle muterende jobs blokkeren totdat hostbeheer `duindorp-staging` en `duindorp-production` afzonderlijk provisiont; de repository voert die hostmutatie niet zelf uit. | Ja, via een nieuw vooraf gereviewd runnerprovisioningcontract. |
| 2026-08-03 | Gebruik uitsluitend een signed, eenmalige live-imageadoptie voor de overgang vanaf production-SHA `a79c8d8…`. | De legacyhealth mist `artifactDigest`, de oorspronkelijke Actions-artifacts zijn verlopen en de legacyimage bevat geen huidige schedulerentrypoint; rebuilden of een algemene bypass vervalst bewijs. | Read-only productiecapture bindt het werkelijke containerimage, de historische quoted runtime wordt zonder `eval` raw genormaliseerd en providers staan uit. Staging bewijst kandidaat→legacy-app met gestopte scheduler→kandidaat met gezonde scheduler. Exacte historische run, OIDC-signature en adoption hash/run-ID zijn verplicht; een eerdere succesvolle adoptierun sluit iedere herhaling uit. | Nee voor een andere SHA; iedere andere overgang vereist een nieuw expliciet besluit. |
| 2026-08-03 | Accepteer in deploy- en acceptatieworkflows uitsluitend de actuele `main`-SHA; een rollback is een nieuwe revert-/forward-fixcommit. | Een vrije oudere-SHA-redeploy koppelt workflowcode, OIDC-identiteit en payload aan verschillende commits en kan secrets vóór betrouwbare validatie bereiken. | Iedere handmatige stagingflow gebruikt eerst een secrets-vrije trusted-main-preflight en production controleert actuele `main` opnieuw vóór en na approval. | Ja; een nieuwe revertcommit doorloopt opnieuw de volledige keten. |
| 2026-08-03 | Onderteken `SHA256SUMS` keyless met Sigstore/Cosign via GitHub Actions OIDC en bind daarmee image, releasemanifest en SPDX-SBOM. | GitHub-native artifactattestaties voor een private repository vereisen Enterprise Cloud, terwijl het actuele organisatieplan Free is. | De plan-onafhankelijke bundle bevat certificaat, handtekening en transparency-logbewijs; staging en production verifiëren exact workflowidentity en issuer vóór checksums. | Ja; na een planupgrade kan een aanvullende native attestation worden ingevoerd zonder de bestaande bundle ongeldig te maken. |
| 2026-08-03 | Gebruik voor SendGrid-webhookbeheer een acceptance-only adminkey en bind zowel beheer als Mail Send aan een vooraf gecontroleerde stagingaccountfingerprint. | Een per ongeluk gedeelde productionkey zou anders webhookinstellingen of echte mail op het verkeerde account kunnen muteren. | `GET /v3/user/username` moet vóór iedere PATCH/send exact dezelfde SHA-256 van `username:user_id` opleveren; accountgegevens worden niet gelogd. | Ja, door keys en de buiten de workflow vastgestelde fingerprint gecontroleerd te roteren. |
| 2026-08-11 | Gebruik voor het verscheepte runtime-image een digest-gepinde, shell-loze `distroless/nodejs22-debian13:nonroot`-basis en houd de glibc Node 22-builder als afzonderlijke buildstage. | Het volledige Debian 12 Node-image nam npm en niet-benodigde OS-pakketten mee en faalde daardoor na de merge op de onveranderde 0-HIGH/0-CRITICAL Trivy-gate. Debian 13 distroless is lokaal met dezelfde scanner en database op nul bevindingen bewezen; Debian 12 distroless en Trixie slim voldeden niet. | Geen productwijziging. De app en scheduler delen exact hetzelfde minimale non-root-image, healthchecks gebruiken de absolute Node-entrypoint en Sharp/libvips wordt expliciet uit de gepinde builddependency meegenomen. De releasegate blijft strikt en krijgt geen uitzondering. | Ja, alleen naar een opnieuw digest-gepinde Node 22 glibc-runtime die dezelfde container-, Sharp- en nulbevindingengates doorloopt. |
| 2026-08-11 | Laat de PostgREST-deploygate uitsluitend vaste interne contractcodes en begrensde transportcategorieën rapporteren, en probe iedere nieuwe releasekritieke service-RPC na een expliciete forward-only schema-reload. | De checker gebruikte één verkeerde named argument-key en maskeerde de resulterende 83-teken contractcode als `REQUEST_FAILED`; de laatste password-recovery-RPC was bovendien na de laatste expliciete cache-refresh toegevoegd. | De echte RPC-signature wordt integraal getest; DNS, TLS, connectie en timeout blijven onderscheidbaar zonder URL, headers, responsebody, credentials of persoonsgegevens te loggen. De contractversie `20260803244000` en alle bestaande rechten blijven ongewijzigd. | Ja, via een volgende forward-only checker- en refreshrevisie. |
| 2026-08-11 | Start stagingrelease `a846c059bce3d7e794504acca57a4771dfdb536d` nooit meer nadat het Phase-B-schema is toegepast, ook niet als automatische applicatierollback. | De oude app roept ingetrokken legacy uitgifte-/QR-RPC's aan en kan daarom niet veilig op het reeds gemigreerde schema draaien. | De deploy blokkeert als die oude app nog draait. Als kandidaathealth faalt, worden kandidaatapp en scheduler fail-closed gestopt; de database blijft forward-only en de incompatibele image wordt niet gestart. | Niet voor deze exacte release/schema-overgang; herstel vereist een forward-fix. |
| 2026-08-11 | Bewijs iedere Caddy-bodycap met een kortlevende HMAC-no-opdrain op het bestaande routepad en een exact-max/max+1-paar. | Caddy 2.10 handhaaft `max_size` lazy tijdens downstreamread; een anonieme route die al op Origin, auth, featureflag of content-type antwoordt kan daarom een actieve cap niet bewijzen. Host-Caddy wijzigen of requestbuffering activeren is buiten scope en vergroot resourcegebruik. | De applicatiebranch is side-effectvrij en retourneert nooit `413`. Exact max moet de gemarkeerde branch bereiken; max+1 moet markerloos door Caddy worden gestopt. Publieke Host/X-Forwarded-Host/-Proto worden tegen `APP_BASE_URL` geverifieerd omdat Next achter de proxy een interne request-URL gebruikt. De middlewareclonegrens is exact `12000001`, terwijl Caddy en routes de productcaps houden. Het bronsecret verlaat runner/app niet en ieder partieel, verlopen, van `Content-Length` voorzien of gecomprimeerd bewijs faalt vóór body-read. | Ja, door een afzonderlijk probe-secret of een dedicated gematcht canarypad in een later gezamenlijk app/hostconfigbesluit. |
| 2026-08-11 | Laat het immutable app-image zelf een minimale, loopbackgebonden Node-bodygateway vóór Next.js starten en behandel de gedeelde host-Caddy uitsluitend als aanvullende grens. | Exacte stagingprobe `31515141010` bereikte bij `128001` chunked bytes de app en bewees dat de repositoryreferentie niet op de gedeelde host actief is. Hostconfiguratie wijzigen is niet geautoriseerd en mag geen verborgen releasevoorwaarde zijn. | Hetzelfde gescande image handhaaft vóór iedere Next-side-effect de vier gedeelde decimale caps, geïsoleerde routegroeppools met samen maximaal `37632000` bufferbytes, tijd/fragmentatie en headernormalisatie. Next luistert alleen op `127.0.0.1:3001` en behoudt een onafhankelijke, fijnmazigere parsergrens. De publieke deployprobe blijft exact-max/max+1 fail-closed bewijzen; een optionele Caddycap mag alleen eerder blokkeren. | Ja, via een opnieuw gescand immutable entrypoint met gelijkwaardige pre-parsergrenzen; een hostwijziging blijft een afzonderlijke hostbeheerbeslissing. |
| 2026-08-11 | Canonicaliseer de GitHub `upload-artifact`-resultaatdigest in de centrale stagingattestlaag. | De action-uitvoer gebruikt kale 64-hex SHA-256, terwijl de GitHub artifact-API en het interne bewijscontract `sha256:<hex>` gebruiken. Losse workflowprefixes zouden dezelfde grens op negen plekken dupliceren. | Alleen exact 64 lowercase hex of exact `sha256:` plus die hex wordt geaccepteerd en altijd met prefix opgeslagen. Alle deploy-, acceptatie-, provider-, restore- en rollbackattesten blijven hetzelfde strikte schema en dezelfde bytebinding gebruiken. | Ja, als een toekomstige action/API één aantoonbaar ander, expliciet gemigreerd digestcontract levert. |
| 2026-08-11 | Bootstrap Node expliciet in iedere self-hosted cleanupjob en vertrouw niet op globale hostsoftware. | De vaste geïsoleerde stagingrunner heeft bewust geen globale `node`; dry-run `31535542508` stopte daardoor fail-closed vóór databaseconnectie. | Dry-run en apply installeren na boundarybewijs en vóór alle Node-scripts dezelfde commit-gepinde Node 22-setup als de releaseketen. Er worden geen hostpackages of runnerinstellingen gewijzigd. | Ja, via een opnieuw gepinde repositoryruntime met gelijkwaardige workflowtests; hostprovisioning blijft buiten scope. |
| 2026-08-14 | Bind een voorraadtekortactie aan productvariant en seizoen, niet aan de operationele mutatie die de projectie ververst. | Betaling, scheduler, levering en acceptatie kunnen dezelfde tekortepisode transactioneel na elkaar verversen. Een wisselend `source_type` botst dan terecht met de vaste dedupe-key en kan voorraadallocatie blokkeren. | `low_stock` en `out_of_stock` gebruiken altijd `article_variant` plus variant-ID als actiebron; de concrete trigger blijft aantoonbaar in het immutable voorraad-/allocatiejournaal. Bestaande actieve episodes worden forward-only genormaliseerd en gereconcilieerd. | Ja, alleen via een nieuwe action-itemprojectieversie; episodes en domeinjournaal blijven behouden. |
| 2026-08-14 | Selecteer bij een GitHub Actions-rerun releasebewijs binnen het tijdvenster van de actuele producerende job. | GitHub bewaart gelijknamige artifacts uit eerdere runpogingen. Een globale exact-één-naameis maakt een veilige rerun onbruikbaar; zomaar het nieuwste artifact vertrouwen mist een cryptografische jobbinding. | Iedere vereiste job blijft exact eenmaal groen in `filter=latest`. Per artifactnaam moet exact één niet-verlopen artifact tussen `started_at` en `completed_at` van de actuele build/deploy/acceptatiejob liggen; resultaat en attest blijven daarnaast aan runpoging, artifact-ID, digest en inhoudshash gebonden. Historisch bewijs wordt niet verwijderd. | Ja, bij een nieuw expliciet artifactattemptcontract met gelijkwaardige binding en regressietests. |
| 2026-08-12 | Dwing cleanup-database-TLS af door de bestaande omgevingssecret in-memory te normaliseren en laat de targetvalidator hetzelfde gesloten URL-contract opnieuw bewaken. | Dry-run `31540435039` vond veilig dat de URL geen expliciete `sslmode` bevat. De secretwaarde kan en mag niet worden uitgelezen; `sslmode` alleen is onvoldoende omdat libpq via queryparameters host/socket/credentials/service of een multi-host-URI het werkelijke doel kan overschrijven. | Eén korte step-scoped grens leest de ruwe secret, staat alleen `sslmode` en begrensde `connect_timeout` toe, weigert downgrade/duplicaten/overrides/multi-hosts, voegt uitsluitend bij afwezigheid `sslmode=require` toe, maskeert de afgeleide waarde en schrijft haar naar GitHub `GITHUB_ENV`. De targetvalidator herhaalt de allowlist; alle volgende dry-run/applystappen erven alleen die waarde. | Ja, door de secret later zelf expliciet met `require`, `verify-ca` of `verify-full` op te slaan; de normalisatie blijft dan semantisch ongewijzigd. |
| 2026-08-12 | Behandel `supabase_functions_admin` als exact gespiegelde optionele herstelrol naast elf verplichte Supabase-kernrollen. | Hosted Supabase maakt deze rol capability-afhankelijk; staging heeft haar niet, terwijl het oude vaste twaalfrollencontract een verder exacte netwerkloze restore blokkeerde. | De volledig gevalideerde bron bepaalt uitsluitend een literal boolean. Cleanup en restore-drill conditioneren create, alter en grant; ontbrekende kernrollen, onbekende of dubbele rollen en ieder volledig inventarisdigestverschil blijven fail-closed vóór een mutatie. | Ja, wanneer een toekomstige bewezen Supabase-baseline de kernset wijzigt; dit vereist een expliciete contract- en testwijziging. |
| 2026-08-12 | Spiegel `postgres → supabase_realtime_admin` afzonderlijk uit de volledig gevalideerde broninventaris. | De rol moet voor dump-/ACL-restore beschikbaar blijven, maar hosted staging verleent dit lidmaatschap niet; de hardcoded grant veroorzaakte terecht exacte restoredrift. | Alleen dit bekende optionele membership krijgt een literal bronboolean. Alle kernmemberships blijven verplicht, overige memberships zijn gesloten, de Functions-rol blijft aan haar postgres-membership gekoppeld en de volledige bron/restore-inventaris moet bytecanoniek gelijk zijn. | Ja, uitsluitend via een expliciete allowlist-/contractwijziging met hosted en netwerkloos herstelbewijs. |
| 2026-08-14 | Start geen Mollie-refund vanuit het portaal of het stagingacceptatieharnas; voer refunds extern in Mollie uit en reconcileer ze uitsluitend via de bestaande webhookgrens. | De producteigenaar heeft dashboardrefunds expliciet buiten scope geplaatst. Mollies asynchrone testrefund bleef bovendien niet-deterministisch `pending`, terwijl refundinitiatie al geen productroute had. | De betaal-/readiness-/mismatch-/replayacceptatie blijft verplicht. Een externe `processing`/`refunded`-observatie wordt server-side opgehaald, zet de betaling fail-closed op refunded en trekt QR-toegang in. De bestaande beheerderregistratie voor reeds extern uitgevoerde kas-/pin-correcties creëert geen Mollie-refund. | Ja, alleen via een nieuwe expliciete productbeslissing en een afzonderlijk geautoriseerd refundontwerp. |
| 2026-08-15 | Behandel extra door SendGrid gerapporteerde runtime-scopes niet langer als releaseblocker; vereis wel aantoonbaar `mail.send`. | De producteigenaar heeft de stagingkey herhaaldelijk in het SendGrid-dashboard gecontroleerd en de exacte-één-scopeprobe bleef blokkeren. | Key- en accountfingerprints, vaste afzender, Mail Send, één echte inboxbezorging, signed Event Webhook, idempotentie, cleanup en post-delivery health blijven verplicht. De aparte scopeworkflow is read-only en wijzigt geen providerkey. Provider-least-privilege blijft een externe beheercontrole en wordt niet langer door deze releaseketen bewezen. | Ja, door later opnieuw een betrouwbare providerspecifieke keyrechtenprobe verplicht te stellen. |
| 2026-08-15 | Laat de beheerderstestmail de SendGrid-gate bewijzen vóór de operationele `mail_templates_v2`-cutover. | De testdeliveryroute vereist bewust een gepubliceerde template/branding en de runtimeprovider, maar maakt geen ledenevent of queuejob. De acceptatie eiste daarnaast ten onrechte dat de databasefeature al actief was en maakte providerbewijs daardoor circulair afhankelijk van de nog te valideren cutover. | Alleen de overbodige featureflag-eis vervalt. Gepubliceerde snapshots, AAL2, vaste testinbox, runtime `EMAIL_ENABLED`, key-/accountbinding, browsermutatieguard, inbox, signed event, idempotentie en cleanup blijven verplicht. Operationele ledenmail blijft door de afzonderlijke databasecutover beheerst. | Ja, als een latere testdeliveryversie een afzonderlijke expliciete acceptanceflag krijgt zonder operationele mail te activeren. |
| 2026-08-15 | Supersedeer handmatige IMAP-inboxcontrole door exact app-HTTP-acceptatie, request-ID-replayhergebruik en een vers cryptografisch geldig SendGrid `delivered`-event voor dezelfde delivery. | De producteigenaar bevestigt dat verzending/ontvangende-serveracceptatie het vereiste releasebewijs is en wil geen mailboxcontrole uitvoeren. IMAP gaf bovendien een timeout nadat Mail Send en replay al waren geaccepteerd. | Schema v2 legt afzonderlijk HTTP-acceptatie, één replay, één delivery-ID, signed-eventbinding, nul failure/quarantine, MFA, fixturecleanup en finale health vast. IMAP-variabelen, -secrets en dependency vervallen. Dit bewijst acceptatie door de ontvangende mailserver, nadrukkelijk niet plaatsing in Inbox of Spam; bounce/dropmonitoring en supportfallback blijven verplicht. | Ja, door later opnieuw een beheerde inboxplacementtest toe te voegen zonder de huidige provider- en eventgrenzen te verzwakken. |
| 2026-08-15 | Sta uitsluitend voor legacy-SHA `a79c8d8…` een eenmalige manifestgebonden lokale-imagecapture toe wanneer de productionrunner exact nul appcontainers ziet. | De geïsoleerde runner ziet geen live container, historische artifacts zijn verlopen en rebuilden zou vals provenancebewijs geven. Zonder rollbacktarget kan de eerste immutable promotie niet veilig worden uitgevoerd. | De deploylock wordt vóór manifest/revision genomen. De fallback eist dezelfde user-owned mode-0600-state, daemonidentiteit, tag/config/manifest/alle layerdigests, releaselabel, publieke én loopbackhealth en bytegelijke state vóór/na; hij voert geen Docker-mutatie uit. Signed evidence noemt `live_container_bound=false` en `one-time-local-manifest-provenance-exception-v1`, zodat dit niet als live-containerprovenance wordt voorgesteld. Staging bewijst daarna nog steeds kandidaat→legacy→kandidaat. | Nee voor een andere SHA of na één geslaagde adoptie; iedere vervanging vereist nieuw expliciet supply-chain- en rollbackbesluit. |
| 2026-08-16 | Sla een expliciete variantalias die na veilige normalisatie gelijk is aan het eigen maatlabel of de eigen leverancierscode niet afzonderlijk op. | Label en code zijn al exacte importmatchsleutels. Dubbele opslag voegt geen herkenning toe en maakte de eerste variant ten onrechte onmogelijk, terwijl echte ambiguïteit uitsluitend tussen aanvullende of andere-variant-sleutels bestaat. | Request en database gebruiken hetzelfde NFKC-/spatie-/casecontract. Alleen redundante eigen aliassen vervallen; dubbele aanvullende aliassen en iedere botsing met een andere variant blijven blokkeren en audit telt uitsluitend werkelijk opgeslagen aliassen. | Ja, via een nieuw forward-only match-keycontract dat cross-variantexactheid en historie behoudt. |
## D-091 — Begrensd negeren van optionele importconflicten

- Besluit: de beheerder kiest dit per mappingrevisie; standaard blijft de strikte modus actief.
- Negeerbaar zijn uitsluitend team, tussenvoegsel, geslacht, actiefstatus en productmaat. De ongeldige of onbekende bronwaarde wordt niet duurzaam geselecteerd en maakt geen fictieve variant of actiepunt.
- Externe identiteit, naam, e-mail, DOB, dubbele bronidentiteit en ambigue ledenmatching zijn niet negeerbaar. Bevestigde of vergrendelde maten blijven altijd beschermd.

## D-092 — Handmatig lid is een kale lid-seizoenregistratie

- Besluit: alleen een beheerder met AAL2 kan een lid handmatig toevoegen. De actieve open seizoenrelatie wordt gemaakt, maar toegang, mail, bestelling, pakket, betaling en maten niet.
- Een exact bestaand Sportlink-ID blokkeert altijd. Een kandidaat op naam+DOB, naam+e-mail of uitsluitend naam wordt nooit automatisch samengevoegd; de beheerder kan na een ongewijzigde preflight bewust een afzonderlijk lid maken.
- DOB blijft in `private.member_sensitive_identity`; auditmetadata bevat uitsluitend booleans, UUID's en payloaddigests.

## D-093 — Pakketten bulk beheren vanuit het ledenoverzicht

- Besluit: alleen een beheerder met AAL2 wijst een pakket toe of trekt het in voor één, meerdere of alle actieve leden. Een expliciete selectie is maximaal vijftig lid-seizoenen; `alle actieve leden` wordt na preflight opnieuw server-side uit het actieve seizoen afgeleid.
- Preview en uitvoering zijn door actie, scope, pakket, seizoen, selectie, reden, databaserevisie en een tien minuten geldig ondertekend bewijs aan elkaar gebonden. De uitvoeringsrequest heeft daarnaast een duurzame idempotentiesleutel en aggregeerde plus individuele auditregels.
- Alleen `confirmed` en `locked` maatkeuzes met een actieve echte productvariant materialiseren als orderregel. `imported_unconfirmed`, leeg en conflict blijven zonder logistieke regel, zodat nooit een maat of SKU wordt gegokt.

## D-094 — Pakket verwijderen is een omkeerbare commerciële withdrawal

- Besluit: een toegewezen pakket wordt niet hard verwijderd en `package_revision_id`, prijs of snapshots worden niet geleegd. De order krijgt `package_assignment_state = withdrawn`; open backorderregels worden ingetrokken en historische snapshots blijven immutable.
- Withdrawal is uitsluitend toegestaan vóór iedere betaling, harde reservering, allocatie, afhaalklaarstatus of uitgifte. Databaseguards blokkeren nieuwe betaling en logistieke activiteit zolang het pakket ingetrokken is.
- Een latere veilige toewijzing heractiveert dezelfde lid-seizoensorder via de bestaande pakketselectiekern en legt een afzonderlijke `package_order.reactivated`-audit vast.

## D-095 — Betaling en maatcommunicatie zijn pakket-snapshot-first

- Besluit: een actieve commerciële pakkettoewijzing met immutable prijs en inhoud is voldoende voor Mollie-checkout. Maatbevestiging, materialisatie van logistieke regels, voorraad en reservering zijn nadrukkelijk geen betaalvoorwaarden.
- De QR- en uitgiftegrens verandert niet: zonder volledige betaling en harde afhaalklare reservering ontstaat geen actieve afhaal-QR en geen uitgifte.
- Maat- en betaalcommunicatie worden uit pakketitems plus actuele maatprofielen afgeleid. Ontbrekend/conflict gaat naar het invulsegment; volledig geïmporteerd-onbevestigd naar het controlesegment; complete bevestiging stopt beide. Logistieke communicatie blijft orderregelgebaseerd.
- Tijdens dual compatibility blijven legacy-orders met niet-geannuleerde regels hun bestaande betaal- en mailcontract houden; nul-regelgedrag is uitsluitend voor een actieve first-class pakkettoewijzing.

## D-096 — FIFO groeperen is uitsluitend een presentatielaag

- Besluit: de voorraadworkspace groepeert zichtbare wachtlijstregels op de stabiele `orderId`, nooit op naam. Naamgenoten en verschillende seizoensbestellingen blijven daardoor afzonderlijk.
- Iedere onderliggende orderregel behoudt haar eigen volgorde, maatgeldigheid, betaalgeschiktheid en FIFO-tijdstip. Voorraadallocatie blijft regelgebonden en wordt niet naar een lidbrede positie omgezet.
- Product en maat komen uit de immutable orderregelsnapshots; de actuele variant levert alleen de optionele SKU. Een native `details`/`summary` houdt de rij mobiel compact en standaard toetsenbordbedienbaar.
- `PUT` behoort tot de toegestane mutatiemethoden, maar krijgt exact dezelfde Origin-, Host-, proxy-, Fetch Metadata-, CSRF- en bodyvalidatie als POST, PATCH en DELETE.

## D-097 — Identiteit en operationele maten hebben gescheiden beheergrenzen

- Alleen een beheerder met AAL2 mag naam, ouder-e-mail, geboortedatum, geslacht en het actieve team wijzigen. De kledingcommissie houdt uitsluitend de operationele maatbevoegdheid; uitgifte en leverancier krijgen deze mutaties niet.
- Het Sportlink-relatienummer is geen generiek profielveld maar een primaire externe importidentiteit en blijft daarom alleen-lezen. Een gewijzigd e-mailadres muteert geen ouderaccount, grant of sessie; portaaltoegang blijft een expliciete afzonderlijke beheeractie.
- Een ongereserveerde bestelde maat mag geauditeerd worden gecorrigeerd. Een harde reservering mag alleen door een beheerder met expliciete vrijgave worden losgelaten; de oude voorraadboeking krijgt een immutable journaalevent en de vervangende regel start opnieuw als nalevering. Uitgegeven maten zijn definitief vergrendeld.
- Profiel- en maatwrites zijn request-idempotent, gebruiken dezelfde lid-/lid-seizoenlocks als import en bewaken een inhoudsrevisie. Auditmetadata noemt alleen gewijzigde velden en technische identifiers, niet de oude of nieuwe persoonsgegevens.

## D-098 — Containerliveness en operationele readiness zijn afzonderlijke poorten

- De Compose-healthcheck bewijst uitsluitend dat het Next.js-proces via `/api/live` antwoordt. Zij beoordeelt geen queue-, scheduler-, provider- of voorraadstatus.
- De release blijft daarna verplicht de bestaande volledige `/api/health` op loopback en publiek, schedulerhealth, runtime-secretbinding, database-/RPC-contract en edge-bodylimits bewijzen voordat revision en manifest worden gepubliceerd.
- Deze scheiding voorkomt dat de scheduler afhankelijk wordt van een readinessstatus die alleen door een schedulercyclus kan herstellen. Een blijvende operationele fout blijft fail-closed; alleen de eerste herstelcyclus krijgt een begrensde langere wachttijd.

## D-099 — Actuele mailconfiguratie en historische OTP-incidenten blijven afzonderlijke healthassen

- Append-only OTP-pogingen met `configuration_error` of `disabled` blijven volledig bewaard voor audit en monitoring, maar tellen niet dubbel als actueel verzendincident: de publieke en interne health controleren reeds rechtstreeks de actuele runtimeflag, databasecutover, providerconfiguratie en sleutelbinding.
- Een recente `provider_rejected` of `render_failed` OTP-poging blijft wel releaseblokkerend. Ook delivery uncertainty, bounce/drop/failure, quarantaine en actuele runtime-/databaseafwijkingen blijven ongewijzigd fail-closed.
- Compose gebruikt `/api/live` voor actuele images. Alleen wanneer een aantoonbaar vorige image die route nog niet kent (`404`) mag de healthcheck éénmalig terugvallen op `/`; iedere andere niet-successtatus blijft een harde fout. De volledige `/api/health` blijft vóór publicatie van revision en manifest verplicht.

## D-100 — Ouderportaalcopy is ledengericht en gepubliceerde mailinhoud blijft beheerbaar

- De door de producteigenaar aangeleverde Nederlandse teksten vervangen technische termen als lid-seizoen, harde reservering, Sportlink-herkomst en pakketmaat waar die voor ouders zichtbaar waren. De onderliggende status-, autorisatie- en veiligheidscontracten wijzigen niet.
- De aangeleverde verschrijving `teneuportaal` wordt als `tenueportaal` uitgevoerd. De OTP-systeemfallback en uitsluitend onaangeraakte systeemconcepten krijgen het nieuwe onderwerp en de kortere beschermde waarschuwing.
- Een door medewerkers gewijzigd of gepubliceerd mailtemplate wordt niet stil door een migratie overschreven; templatebeheer blijft de actuele inhoudsbron. De browseracceptatie bewaakt dat de interne Mollie-disclaimer niet meer aan ouders wordt getoond.

## D-101 — De publieke stagingorigin verhuist integraal naar het clubdomein

- De bestaande stagingruntime, stagingdatabase, poort `14000`, runner en omgevingsidentiteit blijven ongewijzigd; uitsluitend de canonieke publieke origin wordt `https://duindorpsv.dgwebservices.nl`.
- Deploy-, health-, bodylimit-, provider-, rollback- en browseracceptatie gebruiken exact dezelfde nieuwe origin. Een alias via alleen Caddy is onvoldoende, omdat muterende routes `APP_BASE_URL`, `Host` en forwarded headers fail-closed vergelijken.
- De afzonderlijke repository-productionomgeving blijft `https://duindorp.dgwebservices.nl` op poort `24000` en wordt door deze cutover niet gewijzigd.
