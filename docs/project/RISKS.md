# Risks

| ID | Risk | Impact | Mitigation | Status |
| R-001 | Local Supabase staff Auth/MFA and app-local public configuration were not connected. | Staff login and rolgebonden backofficeviews waren niet end-to-end bewezen. | TOTP is projectlokaal geconfigureerd; providerintegratie en volledige browserflow met zelfopruimend testaccount zijn groen. | Closed |
| R-002 | Foundation dashboard was a visual fixture, not an operational data view. | Users could mistake sample metrics for live operations if released. | Dashboard now reads one AAL2-protected aggregate, rejects invalid responses and has browser assertions that ban the former fixturecopy. | Closed |
| R-003 | Inventory/QR migrations had not run on a clean local PostgreSQL instance. | SQL syntax, RLS behavior or concurrency semantics could fail despite static review. | Clean reset, 18 pgTAP assertions and a real two-session race test now pass on the isolated local stack. | Closed |
| R-004 | Native `BarcodeDetector` support differs per browser. | Camera scanning can be unavailable on some issue devices. | Keep the secure token/link input fallback; validate the selected club device matrix and add a project-local scanner library only if required. | Open |
| R-005 | Supabase CLI bindt lokale ontwikkelservices aan alle hostinterfaces; Studio en pgMeta hebben lokaal geen eigen login. | Een onjuist gefirewallde ontwikkelserver kan lokale tooling en fictieve data aan het netwerk tonen. | Poorten niet publiek publiceren, hostfirewall toepassen en `pnpm supabase:stop` gebruiken buiten actieve ontwikkelsessies. | Open |
| R-006 | Het afzonderlijke ledenoverzicht toont nog geïsoleerde previewdata. | Een medewerker kan dit scherm ten onrechte als een actuele operationele ledenlijst interpreteren. | De previewmodule is verwijderd; lijst, tellingen, filters, detail, orders en historie komen uit AAL2- en rolbeveiligde RPC's met strikte responsecontracten. | Closed |
| R-007 | Nieuwe service-only QR- en betaal-RPC's hadden functierechten maar geen schema-USAGE via de lokale Data API. | Server-side beheeracties retourneerden 403 ondanks geldige AAL2/RBAC en PostgreSQL-functierechten. | `service_role` heeft nu alleen `USAGE` op `app` en expliciete `EXECUTE` op vertrouwde RPC's; pgTAP en de echte browserroute bewijzen de grens. | Closed |
| R-008 | Een e-mailjob kan na provideracceptatie maar vóór lokale completion in `processing` blijven staan. | Het bericht is mogelijk verzonden, terwijl automatisch opnieuw proberen een dubbele mail kan veroorzaken. | Ambigue transportuitkomsten worden `delivery_uncertain` en nooit automatisch geclaimd. Signed provider-events reconciliëren zonder resend; alleen een AAL2-beheerder kan met providerbewijs, attestatie, optimistic concurrency en audit veilig bevestigen of opnieuw vrijgeven. | Closed in code; staging fault/recovery evidence pending |
| R-009 | Live SendGrid-domeinverificatie en een volledige Mollie test-mode roundtrip vereisen externe accounts en publiek bereikbare HTTPS-webhooks. | Lokale mocks en contracttests bewijzen de codegrenzen, maar niet de echte providerconfiguratie of deliverability. | Mollie paid, replay, mismatch en refundaanmaak zijn groen in stagingrun `29918711338`; feature flags blijven standaard uit en SendGrid vereist nog Mail Send/inbox/eventbewijs vóór productie. | Mollie staging closed; SendGrid open |
| R-010 | Een echte back-up/restore-oefening kan niet lokaal het beheerde stagingback-uppad, EU-regio en operationele RTO/RPO bewijzen. | Datacorruptie of foutieve migratie kan in productie langer herstel vereisen dan voorzien. | De handmatige stagingworkflow maakt een actuele logical backup, herstelt die naar een netwerkloze digest-gepinde PostgreSQL 17-container, controleert schema/constraints/geaggregeerde tellingen en wist dump/container altijd. Run `29874124335` slaagde binnen RPO/RTO op exacte staging-SHA en bewaarde uitsluitend geredigeerd bewijs. | Closed for staging; production drill follows production approval |
| R-011 | Stagingruntime, scheduler, alerts en secretstore kunnen door omgevingsdrift afwijken. | Een groene lokale releasecandidate kan door ontbrekende heartbeat of foutieve proxyheaders alsnog falen. | Staging SHA `76bfe4ef…` is publiek groen voor deploy, kernacceptatie en restore; Compose-scheduler, runledger en 503-health zijn aanwezig. Configureer en bewijs nog de externe heartbeat. | Partially open; external heartbeat pending |
| R-012 | De e-mailprovider kan een uitnodiging of transactionele e-mail accepteren zonder dat inboxlevering slaagt. | Een medewerker of ouder ontvangt mogelijk geen toegangscode of operationele mail. | Verifieer afzenderdomein, template-ID's, fictieve inbox, bounce/eventwebhook en supportfallback expliciet in staging. | Open |
| R-013 | GitHub Free biedt voor deze private repository geen protected Environments; persistente self-hosted runners zijn geen schone securityboundary. | Een actor die workflowcode op `main` kan wijzigen, kan een deployrunner targeten en geïnjecteerde repositorysecrets benaderen. | Beperk write/admin, voer nooit PR-code op deployrunners uit, gebruik twee repo-level runners/Linux-users, injecteer productionsecrets pas na de autorisatiejob en upgrade naar protected Environments zodra mogelijk. | Open |
| R-014 | Production environment heeft geen required reviewer; de private repository gebruikt GitHub Free. | De bedoelde vier-ogenapproval bestaat niet, ook al zijn parent-/cronsecrets aanwezig. | Productionpromotie controleert de environment via de API en blokkeert zonder exact `main`, minimaal één required reviewer en `prevent_self_review=true`. Een planupgrade en reviewerconfiguratie blijven extern vereist. | Open external release blocker |
| R-015 | De volledige immutable Dockerflow is nog niet door de bewust geblokkeerde productionstap uitgevoerd. | Production-specifieke runtime-, Caddy- of Supabasedrift wordt pas na handmatige approval live bewezen. | Staging is volledig groen met hetzelfde promotieartefact; behoud de required-reviewer-gate en verifieer production pas na expliciete toestemming. | Open; production approval required |
| R-016 | Docker Engine rapporteerde voor hetzelfde opgeslagen image op de buildrunner de configdigest en op de Rootless containerd-store de OCI-manifestdigest als `.Id`. | De eerste stagingrun stopte vóór migratie en containerstart, ondanks een intact artefact; Caddy hield daardoor een 502 zonder upstream. | Manifest v2 bewaart en verifieert OCI-, config- én artifactdigest, controleert beide blobs en het releaselabel, en accepteert alleen de twee canonieke Docker-ID-representaties. | Closed |
| R-017 | De hosted Supabase Data API exposeerde het gebruikte custom schema `app` niet en retourneerde `PGRST106`. | De container startte correct, maar de operationele healthcheck bleef 503 omdat de service-only health-RPC niet bereikbaar was. | Een vooruitrolbare migration beheert exact `public, graphql_public, app` op de `authenticator`-rol, triggert PostgREST config/schema reload en wordt door pgTAP, repositorycontract en gezonde hosted health bewaakt; `private` blijft uitgesloten. | Closed |
| R-018 | Uitgeschakelde providers leverden lege optionele waarden in `.env.runtime`, die door de getypeerde serverconfiguratie als aanwezige ongeldige waarden werden geweigerd. | Iedere serverconfig-read kon werpen; de publieke root gaf 500 en health bleef 503 ondanks een gezonde container en bereikbare Data API. | Lege optionele waarden worden niet meer geserialiseerd en defensief naar `undefined` genormaliseerd; provider-on regressietests blijven de verplichte velden fail-closed afdwingen en staging is publiek groen. | Closed |
| R-019 | Mollie-servicecalls naar drie functies in schema `app` gebruikten impliciet het `public`-schema. | Hosted checkoutbinding en webhookreconciliatie zouden na een geslaagde providercall falen. | De RPC-client routeert alleen `prepare_mollie_payment` via `public` en alle overige providerfuncties expliciet via `schema('app')`; unit- en databasecontracten zijn groen. | Closed |
| R-020 | SendGrid Mail Send `X-Message-ID` en Event Webhook `sg_message_id` werden als dezelfde identifier behandeld. | Geldige delivery/bounce-events konden niet aan de lokale job worden gekoppeld en werden genegeerd. | De reeds meegestuurde niet-persoonlijke `email_job_id` is leidend; een forward migration verwerkt afwijkende of ontbrekende `sg_message_id` en dedupliceert nog steeds op `sg_event_id`. | Closed |
| R-021 | GitHub Actions-schedules kunnen vertragen en ondersteunen geen native cronfrequentie van één minuut. | De queue kan later dan één minuut worden verwerkt; dit pad is niet geschikt als productie-SLA. | De GitHub-cron is verwijderd. Een geharde scheduler in ieder omgevingsspecifiek Composeproject voert per minuut uit; productionpreflight vereist een onafhankelijke HTTPS dead-man-switch en interne health registreert gemiste runs. | Code closed; production heartbeat secret/run evidence required |
| R-022 | Directe OTP- en provider-smokemails hebben geen duurzame `email_job_id`, terwijl de webhookparser die voor ieder operationeel event verplichtte. | Geldige signed delivery-events kregen `400`, waardoor SendGrid ze kon blijven aanbieden. | Niet-correleerbare maar geldig gesigneerde events worden als `ignored` met `202` geaccepteerd; queue-events blijven strikt op UUID en `sg_event_id` gevalideerd en idempotent verwerkt. | Closed |
| R-023 | De geldige EU-regional staging-key beheert de signed Event Webhook, maar Mail Send retourneert HTTP 401. | Transactionele en bulkmail kunnen niet veilig worden geactiveerd; OTP-aanvragen blijven neutraal gepauzeerd. | De stagingworkflow controleert voortaan expliciet de `mail.send`-scope vóór één beheerde testmail. Vervang of corrigeer de EU-subuserkey wanneer die scope ontbreekt; houd beide e-mailswitches uit tot Mail Send 202 en groene health. | Open; external credential gate |
| R-024 | Handmatige lidinactivatie was alleen via Sportlink-import mogelijk en betaalinitiatie controleerde de actuele lidstatus niet overal. | Een uitgeschreven lid kon via een bestaande bestelling alsnog een nieuwe betaling starten; medewerkers konden de status niet gecontroleerd herstellen. | Een geaudite status-RPC en backofficeactie zijn toegevoegd; ordertoewijzing, handmatige betaling en Mollie-initiatie blokkeren inactieve leden, terwijl bestaande historie en webhookreconciliatie behouden blijven. | Closed |
| R-025 | Teamtoewijzing kan geen individuele maat afleiden omdat er buiten orderregels geen canoniek maatprofiel per lid bestaat. | Automatisch raden kan een hele teamorder met verkeerde maten vullen of bestaande maten overschrijven. | De medewerker kiest per artikel expliciet één variant/maat voor de bulkactie; bestaande artikelen worden altijd overgeslagen en de preview toont aantallen vóór commit. Individuele afwijkingen worden daarna per onbetaalde bestelling beheerd. | Accepted for MVP |
| R-026 | Een losse preview en commit konden door tussentijdse import-, betaal- of seizoenswijzigingen verschillende datasets raken. | De medewerker kon meer of andere leden/orders wijzigen dan in de bevestiging getoond; losse SQL-statements konden inconsistente tellingen opleveren. | Commit vereist een kortlevend HMAC-token met seizoen, actie en SHA-256-snapshot. PostgreSQL vergelijkt die snapshot opnieuw terwijl relevante tabellen tegen gelijktijdige DML zijn vergrendeld en weigert drift met `40001`. Oude ongebonden commit-RPC's zijn ingetrokken. | Closed |
| R-027 | Een maatprofiel naast orderregels kan een dubbele waarheid creëren of een oude groeimaat naar een volgend seizoen meenemen. | Verkeerde maten kunnen worden besteld of uitgegeven als een voorbereidende voorkeur als definitieve besteldata wordt behandeld. | Profielen zijn per seizoen en artikel, gebruiken een catalogusvariant, blokkeren stale writes en kunnen bestaande bestelregels niet wijzigen. Orderregels synchroniseren het profiel en blijven met hun maatsnapshot leidend; er is geen automatische rollover of orderaanmaak. | Closed for individual entry; team-profile automation remains out of scope |
| R-028 | De instellingensingleton bestond voorheen alleen via seed en een `STABLE` getter kon de eigen seizoenmutatie niet terugzien. | Een migrations-only omgeving kon een seizoen wel opslaan maar het niet tonen; de UI meldde dan ten onrechte succes en hield Opslaan geblokkeerd. | De forward migration maakt de singleton idempotent aan, gebruikt een actuele `VOLATILE` workspace, accepteert instellingen zonder actief seizoen en laat de UI alleen succes melden als het seizoen in dezelfde response staat. | Closed |
| R-029 | Nieuwe settings-RPC's waren na een geslaagde hosted migration niet aantoonbaar zichtbaar in de PostgREST-schemacache. | Een geldige AAL2-beheerder zag een generieke settingsfout en kon aanwezige seizoenen niet bekijken of activeren. | Een gegevensneutrale forward migration triggert schema-reload; deployment blokkeert vóór image-activatie op een service-only versieprobe die tevens de drie `authenticated`-functieprivileges controleert. Veilige operationele logging onderscheidt schema-, autorisatie- en responsefouten. | Closed |
| R-030 | De bestaande staging- en productionrunners draaiden aantoonbaar onder dezelfde toegestane `deploy`-principal/Rootless-daemon. | Composeprojecten, poorten en paden zijn geen securityboundary; een gecompromitteerde runner kan siblingcontainers beheren. | Code eist nu vaste runners `duindorp-staging-01`/`duindorp-production-01`, users `duindorp-staging`/`duindorp-production`, private homes/sockets/data-roots/runtimebomen, peer-onbereikbaarheid en root-owned markers. Alle muterende jobs blokkeren tot hostbeheer dit provisiont. | Code closed; VPS owner migration is open release blocker |
| R-031 | De onafhankelijke monitorbestemming is nog niet gekozen; `OPERATIONS_HEARTBEAT_URL` ontbreekt in beide GitHub environments. | Een scheduler- of VPS-storing kan zonder tijdige externe melding blijven. | Kies een beheerde dead-man-switch met escalatie-eigenaar, zet een unieke geheime URL per omgeving en bewijs gemiste ping plus herstel. Productionruntime weigert zonder deze secret te deployen. | Open; external monitoring gate |
| R-032 | Hosted Supabase-verkeer met een staff-user-JWT bleef zowel vanaf de VPS als browser-PostgREST hangen. | Medewerkers kwamen na een geldige MFA-code opnieuw op het inlogscherm en de geautomatiseerde rollenacceptatie liep vast. | De app verifieert handtekening en claims lokaal tegen de publieke ES256-JWKS met vaste issuer, audience, algoritme, lifetime, role en `aal2`; alleen daarna maakt service-role voor het actuele actieve profiel een hash-only, intrekbare sessie. Iedere request controleert vervaldatum, intrekking en actueel staffprofiel. Core-run `29873991736` bewijst op exacte staging-SHA echte TOTP, sessieaanmaak, sessiehergebruik en alle drie rollen. | Closed |
| R-033 | De appcontainer kon de publieke Supabase-JWKS bij een geldig token niet tijdig ophalen. | Lokale cryptografische verificatie bleef alsnog aan container-egress gekoppeld en de MFA-sessieroute hing. | Beide omgevingen krijgen hun openbare ES256/P-256 keyset als gevalideerde GitHub environmentvariable. De runtime gebruikt uitsluitend de environmentgebonden lokale keyset; deployment faalt bij ontbrekende/misvormde keys en signing-keyrotatie is een expliciete operationele wijziging. Exacte-SHA core-run `29873991736` slaagde zonder request-time JWKS-netwerkverkeer. | Closed |
| R-034 | De oude stagingharness wachtte na een snelle foutresponse op Playwrights navigatie/bodyread en maskeerde zo de echte staff-sessiefase als een achtminutenhang. | Een echte autorisatieafwijzing leek op infrastructuuruitval en vertraagde herstel. | De submit wordt zonder navigatiewacht gedispatcht; provider-, request- en responsfasen hebben PII-vrije markers en harde grenzen. De route spiegelt bestaande veilige foutcodes in een no-store header. Run `29861655480` bewees binnen seconden dat ES256/AAL2 slaagt en sessie-uitgifte wordt afgewezen. | Closed |
| R-035 | De stagingharness gaf SQL via `spawnSync.input` aan `docker run`, maar miste `--interactive`; psql ontving daardoor geen statements en sloot toch met status 0. | Het actieve staffprofiel werd niet aangemaakt, waarna een correct geverifieerde AAL2-medewerker geen opaque appsessie kreeg. | Docker-stdin is expliciet gekoppeld en statisch getest. Run `29863954993` bewees daarna service-role create/revoke, MFA-provider 200, app-sessie 200 en landing in de backoffice. | Closed |
| R-036 | De coreharness interpreteerde `GET /api/settings` als autorisatieprobe, terwijl die mutatieroute uitsluitend `POST` ondersteunt. | Een werkende beheerderlogin werd ten onrechte als settings-403 gerapporteerd. | Core bewijst eerst de opaque app-sessie met 200 en opent daarna voor beheerder de echte settingspagina; kledingcommissie wordt gecontroleerd op afwezigheid van de beheerlink. De get/revoke-deployprobes blijven als runtimecontracthardening behouden. | Closed |
| R-037 | De settingsworkspace gebruikte `season.id = active_season_id`; bij een null actieve-seizoenkeuze werd daardoor JSON `active: null` opgebouwd. | Het strikte responsecontract verwierp alle aanwezige seizoenen, waardoor een geldige AAL2-beheerder de settings-errorstate zag en seizoenen niet kon bekijken of activeren. | Forward migration 55 gebruikt expliciete `coalesce(..., false)`. pgTAP test exact meerdere seizoenen zonder actief seizoen; directe AAL2-RPC/Zod-probe en zichtbare instellingenpagina zijn groen in core-run `29873991736`. | Closed |
| R-038 | Een extern in Mollie uitgevoerde refund kan vertraagd van `pending` naar `processing/refunded` gaan. | Lokale betaalstatus en QR mogen niet op een annuleerbare providerstatus vooruitlopen en mogen een terminale refund evenmin missen. | Het portaal initieert geen refund. De webhook haalt payment en officiële refundlijst opnieuw op; uitsluitend `processing/refunded` trekt fail-closed de QR in. Unit-, service- en databasetests dekken pending, terminal, replay en QR-intrekking. | Accepted external-provider operation; reconciliation closed in code |
| R-039 | De Phase B-uitbreiding raakt vrijwel iedere huidige tabel en procesgrens terwijl staging al historische MVP-data bevat. | Een big-bang vervanging kan geld-, voorraad-, uitgifte- of toegangshistorie vermengen of verliezen. | Expand/backfill/reconcile/dual-read/feature-flag/contract in verticale slices; iedere financiële of logistieke reconciliatie-afwijking blokkeert activering. | Open tijdens Phase B |
| R-040 | Geboortedatum is extra gevoelige minderjarigen-PII. | Een brede staffquery, export, log of supplier-/scannerresponse kan onnodig DOB blootleggen. | DOB staat alleen in private opslag en afzonderlijke beheerder- en granted-parent-RPC's; kledingcommissie, uitgifte, supplier, audit en logs krijgen het veld niet. Negatieve DB-, route- en recursieve contracttests bewijzen de grens. | Closed in code; hosted negative evidence pending |
| R-041 | Het opschonen van staging is destructief en staging/productie kunnen vergelijkbare schema's hebben. | Een verkeerd doel of te brede cascade kan medewerkeraccounts of productiegegevens verwijderen. | De workflow vereist exact stagingorigin/ref/DB/TLS/SHA, weigert de production-ref, gebruikt 100 expliciete wistabellen zonder `CASCADE` en behoudt 28 tabellen plus heel Auth. Apply volgt pas na providerstop, encrypted dump, decryptie, netwerkloze restore, digesthercontrole onder lock en staff/Auth/config-postcondities. | Closed in code; hosted wipe is geblokkeerd op runnerisolatie |
| R-042 | De huidige queryparameter-QR kan via browserhistory, reverse-proxy- of referrerlogs lekken. | Een herbruikbare QR-identiteit kan worden gekopieerd en misbruikt. | Fragment/direct-scan, history scrub, random nonce, aparte current/previous QR-keyring, authenticated POST exchange, sessiegebonden single-use grant, gecontroleerde rotatie en logredactie zijn geïmplementeerd. Route-, DB-, concurrency- en echte lokale browsertests bewijzen de grens. | Closed in code; device/staging evidence pending |
| R-053 | Production miste de verplichte huidige QR-keynaam en keyversie. | Runtimevalidatie blokkeert productie; een ongeplande vervanging kan actieve locators onleesbaar maken. | Een unieke 32-byte `QR_TOKEN_PEPPER` is zonder output gegenereerd en `QR_TOKEN_PEPPER_VERSION=1` ingesteld. Current/previous-rotatie blijft verplicht zodra locators bestaan. | Closed for initial environment; hosted health evidence pending |
| R-043 | Bestaande request-bodychecks vertrouwden deels op `Content-Length` en parsten daarna onbeperkt. | Chunked uploads konden memory-, tijd- of parsergrenzen omzeilen. | Alle muterende routes gebruiken benoemde streamlimieten. De Caddy 2.10+-referentie bevat gescheiden edgecaps; de deploygate bewijst per groep met een kortlevend HMAC-no-opcontract zowel exact de decimale grens als grens+1. De appbranch geeft nooit zelf `413`, zodat alleen een markerloze edge-`413` groen is. | Closed in code; exact-SHA stagingbewijs pending |
| R-044 | Productiemigraties publiceerden staging-specifieke Mollie-fixture-RPC's. | Service-rolemisbruik kon testrecords en instellingen in productie muteren. | Migration 60 verwijdert alle vier acceptance-only RPC's en de oude ledgertabel zonder `CASCADE`, maar breekt eerst af op een actieve ledger of orphanfixture. Het target-locked externe SQL-harnas vereist een bestaande open stagingseason plus actieve Mollie-testconfig, wijzigt geen globale instelling en gebruikt uitsluitend een deterministisch fictief ouderaccount met exact twee seizoensgrants en een synthetische runactor. OTP/sessie bewijzen exact die twee leden; cleanup valideert account, actor, seizoen en totaal fail-closed vóór de eerste delete. Acceptance en stagingdeploy delen één serialisatiegroep. DB-CI voert positieve, collision-, migration-guard- en idempotentiescenario's met de echte SQL uit; deploy vereist voor iedere verboden RPC `404/PGRST202`. | Closed in code; nieuwe exact-artifact Mollieacceptatie pending |
| R-045 | De dependency-audit meldde high advisories in Next.js, Sharp/libvips, PostCSS, `nanoid` en meerdere `brace-expansion`-majorlijnen. | SSRF, denial-of-service, file disclosure en native image-parserproblemen waren bereikbaar vanuit build- of runtimepaden. | Pin Next.js 15.5.21, Sharp 0.35.0 en PostCSS 8.5.23; forceer `nanoid` minimaal 3.3.17 en `brace-expansion` minimaal 1.1.18, 2.1.4 en 5.0.9; maak `pnpm audit --audit-level high` verplicht in CI en stagingpreflight. | Closed in code; actuele audit meldt geen bekende kwetsbaarheden, CI evidence pending |
| R-046 | Een catalogusbeheerder kon een artikel uit een seizoen halen, het deactiveren of de laatste actieve maat uitschakelen terwijl een pakkettemplate dat product nog gebruikte. | Een gepubliceerd pakket kon operationeel niet meer aan een echte maatvariant worden gekoppeld, ondanks een intacte commerciële snapshot. | Migration 62 koppelt templateitems met gevalideerde composite season-FK's, beschermt concepten/actieve revisies met catalogustriggers en valideert product plus actieve maattabel opnieuw binnen de publicatietransactie. | Closed |
| R-047 | Een directe omschakeling van member-wide legacy ouderlinks naar lid-seizoengrants kan bestaande gezinstoegang verliezen of een kind uit een ander seizoen onbedoeld ontsluiten. | Legacy links kennen geen seizoen en één link kan historisch door meerdere contexts zijn gebruikt. | De nieuwe grens blijft standaard uit, legacyauth blijft tot cutover intact, iedere legacy link krijgt reviewstatus, multi-seizoengrants delen een legacy link nooit als unieke grantidentiteit en de AAL2-cutover blokkeert op iedere onopgeloste koppeling. | Closed in code; hosted reconciliation/cutover evidence pending |
| R-048 | Case-, Unicode- of alias-equivalente maatcodes konden aan verschillende varianten binnen één product toebehoren of gelijktijdig via oude en nieuwe RPC's worden ingevoerd. | Een import kon dan niet veilig exact bepalen welke fysieke voorraadvariant bedoeld werd. | Genormaliseerde label/code/aliaskeys worden productgebonden gevalideerd, format-/bidi-tekens en `Anders…` zijn verboden, beide RPC-generaties delen dezelfde advisory lock en bestaande ambiguïteit verschijnt expliciet in de beheerworkspace. Dynamic-importcutover zal op ieder resterend conflict blokkeren. | Closed for new writes; existing hosted catalog reconciliation pending |
| R-049 | Een dynamische import moet tijdelijk alle CSV-kolommen kunnen herlezen terwijl niet-geselecteerde data niet duurzaam of breed leesbaar mag worden. | Plaintext staging, sleutelhergebruik, onveilige keyrotatie of een door e-mailfalen geblokkeerde cleanup vergroot de blootstelling van leden-PII en DOB. | Strikte parsergrenzen, aparte 32-byte base64url-rootkey, per-batch HKDF/AES-256-GCM/AAD, private default-deny payloadtabel, service-only read-RPC, expiry per upload, onafhankelijke vijfminutencleanup en geaggregeerde health. V2 sluit legacy route én RPC; RLS houdt v2-metadata beheerder-only; deploy vergelijkt de keyfingerprint en blokkeert rotatie/verwijdering met actieve uploads. Beide featurepoorten staan standaard uit. | Closed in code for upload staging; environment keys and hosted expiry/rotation evidence pending |
| R-050 | Een lid zonder externe ID kan op naam, gedeeld ouderadres of DOB meer dan één plausibele kandidaat hebben. | Een fout-positieve match vermengt seizoenen, pakketten, toegang en minderjarigen-PII. | Geen enkel zwak veld is zelfstandig een identiteit. De importdry-run moet een ondubbelzinnige samengestelde exacte match aantonen; iedere dubbelzinnigheid wordt één blokkend actiepunt en commit slaat die rij over. | Identity foundation closed; dry-run/commit enforcement pending |
| R-051 | Legacy kas- en Mollie-RPC's activeerden een herbruikbare QR direct bij betaling en het TypeScript-contract accepteerde metadata v1 terwijl de database v2 produceerde. | Checkout faalde vóór de provider; een betaald pakket zonder voorraad kreeg toch een scanbaar afhaalbewijs en gelijktijdige kas/webhook kon dubbel geld vereisen. | Nieuwe betalingen bewaren lid-seizoen, pakketsnapshot en metadatarevision; kas is beheerder+AAL2 en request-idempotent; historische v1 en nieuwe v2 reconcile zijn versiegebonden. Beide service-reachable legacy-RPC's zijn ingetrokken en geen nieuw betaalpad maakt een QR. Twee-sessietests bewijzen serialisatie. | Payment code closed; allocation-gated QR and hosted Mollie v2 evidence pending |
| R-052 | Legacy ontvangstregels hebben geen seizoen en kunnen daarnaast reserveringen bevatten die volgens het nieuwe betaal-/maatcontract ongeldig zijn. | Een automatische backfill naar het actieve seizoen of stil behouden van een onbetaalde reservering kan voorraad vermengen en een onterecht afhaalrecht creëren. | Bewezen reserveringen/uitgiftes worden lossless als seizoenprojectie vastgelegd; ongeldige allocaties krijgen `review_required`; vrije ontvangstvoorraad blijft buiten het journaal tot expliciete AAL2-toewijzing. Reconciliatierapport, action items en featureflag blokkeren cutover zolang één kandidaat of reviewallocatie openstaat. | Closed in code; hosted reconciliation and cutover evidence pending |
| R-054 | Gepubliceerde mail-v2-inhoud bewees niet dat ieder proces een doelgroep, suppressie, idempotentie en producer had. | Een inhoudelijk complete catalogus kon de runtime activeren terwijl login-, maat-, betaal-, voorraad- of foutmails nog via legacy of helemaal niet werden geproduceerd. | Een private default-deny capabilityledger wordt uitsluitend forward-only gevuld nadat een producent en zijn negatieve tests bestaan. LOGIN_OTP gebruikt nu een codevrije immutable attemptledger; alle producenten zijn lokaal `19 / 19` en cutover blijft op volledige publicatie plus nul reconciliaties geblokkeerd. | Closed in code; staging Mail Send/inbox/event evidence pending |
| R-055 | Mail Send-acceptatie, Event Webhook-delivery en retry waren voorheen alleen via de mutabele job gekoppeld. | Een crash na provideracceptatie, een late eventreplay of afwijkende SendGrid-identifiers kon een dubbele mail, verkeerd herstel of een statusprojectie op de verkeerde poging veroorzaken. | Iedere claim maakt een immutable attempt-ID; alleen attempt-gebonden v4/v2-RPC's zijn extern bereikbaar. HTTP- en webhookidentiteiten blijven afzonderlijk, conflicten worden atomair gequarantaineerd, late oude events projecteren niet en AAL2-herstel vereist providerbewijs. Health en release-readiness tonen iedere legacy ambiguïteit, quarantaine en ongebonden event. | Closed in code; staging Mail Send/inbox/event/recovery evidence pending |
| R-056 | Een leveranciersportal die bestaande voorraad- of leden-RPC's hergebruikt kan wachtlijst-PII tonen of als vierde personeelsrol onbedoeld bredere rechten krijgen. | Free-Kick kan namen, DOB, team, orders of individuele betalingen zien en seizoenen onderling vermengen. | Een aparte default-deny principal heeft uitsluitend expliciete open-seizoengrants en een strikt read-only aggregate-RPC. Voorraad blijft maatgebonden, vraag is alleen per geslacht, conflicten zijn tellingen zonder ruwe waarde en rotatie/grantwijziging trekt sessies in. pgTAP, route-, contract- en concurrencytests blokkeren PII en zombies. | Closed in code; staging/browser acceptance pending |
| R-057 | De bestaande restore-drill verwijdert zijn dump en kan daarom geen gecommitteerde staging-wisactie terugdraaien. | Een technisch correcte maar bedrijfsmatig onbedoelde cleanup is na commit niet herstelbaar; uploadfalen ná wissen zou ook het laatste herstelbestand kunnen verliezen. | Prepare maakt een volledige logical dump, versleutelt en herstelt die netwerkloos en uploadt backup plus prepared-state eerst als immutable artifact. Apply start pas na succesvolle upload, bindt artifact-ID/checksum/statedigest en weigert drift vóór de transactionele locks. Ruwe dumps en passphrasefiles worden altijd verwijderd. | Closed in code; hosted encrypted-backup evidence pending |
| R-058 | Handmatige workflowinputs konden een niet-gevalideerde ref uitchecken terwijl environmentsecrets al jobbreed beschikbaar waren. | Kandidaatcode kon provider-, cron- of databasecredentials uitlezen vóór een betrouwbare SHA-/deploycontrole. | Een herbruikbare control-plane workflow checkt zonder environment of secrets vaste `main` uit, eist exact actuele 40-hex SHA, canonieke deployrun, manifest en deployattest en geeft uitsluitend gevalideerde outputs door. Environmentjobs checken alleen die output uit; gevoelige providersecrets zijn daarnaast step-scoped. | Closed in code; hosted workflow evidence pending |
| R-059 | GitHub-native artifactattestaties waren niet beschikbaar en `main`/production misten aantoonbare vier-ogenbescherming. | Production kon zonder betrouwbare commit- en approvalgrens worden gemuteerd. | Artifactprovenance gebruikt keyless Sigstore/Cosign. `main` vereist nu beide CI-checks, één last-push-approval en adminhandhaving; production heeft één required reviewer en is met `prevent_self_review=true` bijgewerkt. De kandidaatpreflight verifieert dit opnieuw fail-closed. | Closed in control plane; hosted candidate evidence pending |
| R-060 | Een geldige SendGrid-key kan bij een ander account of een productionaccount horen. | Stagingacceptatie kan echte webhookinstellingen of mail op het verkeerde account wijzigen. | Dedicated staging-adminkey, runtime Mail Send-key en vooraf gecontroleerde SHA-256 van `username:user_id`; iedere webhook-PATCH en acceptance-send weigert een mismatch zonder accountgegevens te loggen. | Closed in code; staging keys/fingerprint/inbox evidence pending |
| R-061 | De actuele productionrelease heeft legacyhealth zonder `artifactDigest`, een historisch gequote runtimebestand en geen schedulerentrypoint; de oorspronkelijke releaseartifacts zijn verlopen. | Een eerste promotie kan bij fout niet bytebewijsbaar of runtimecompatibel terugrollen; het starten van de huidige scheduler met de oude image faalt. Rebuilden of een generieke bypass vervalst supply-chainbewijs. | Een afzonderlijke eenmalige workflow verifieert historie en bindt een zichtbare container, of expliciet de eenmalige manifestgebonden lokale fallback, aan OCI/config/alle layers, releaselabel, daemonstate en publieke plus loopbackhealth vóór/na. Zij normaliseert runtime zonder `eval`, schakelt providers uit, bewijst kandidaat→legacy-app→kandidaat, tekent via OIDC en blokkeert hergebruik. | Closed in code; exact-main hosted adoption evidence pending |
| R-062 | Workflowbewijs kon onbeperkt oud zijn en het productieherstelartifact werd na upload niet teruggedownload. | Een oude groene run of mislukte artifactopslag kon als actuele releasegate gelden. | Alle acht runs zijn uniek, maximaal 48 uur oud en na deploy gestart; attestatietijden liggen in hun runvenster. Het encrypted herstelpunt wordt vóór migratie via artifact-ID teruggedownload en bytegelijk bewezen. | Closed in code; hosted evidence pending |
| R-063 | Supabase dashboard-passwordrecovery viel terug op de algemene Site URL, terwijl de applicatie alleen `type=invite` accepteerde en een bestaande MFA-factor AAL2 voor password-update vereist. | Een medewerker kon het wachtwoord niet herstellen of kon op het ouderportaal landen; een onzorgvuldige generieke resetroute kan accounts enumereren of bestaande app-/scannersessies actief laten. | Aparte staff-recoveryroute met neutraal antwoord, IP-/adreslimiet, strikt recoveryfragment, bestaande TOTP, minimaal 16 tekens, globale Supabase-signout, service-only intrekking van alle opaque sessies/exchanges/scangrants en PII-vrije audit. | Closed in code; hosted Redirect URL en echte E2E-accountacceptatie pending |
| R-064 | Het release-image gebruikte een volledig Debian 12 Node-runtime met npm en overbodige OS-pakketten. | Deployrun `31434321788` faalde na de merge op 17 HIGH/5 CRITICAL OS-bevindingen en 7 HIGH/1 CRITICAL npm-bevindingen; staging bleef daardoor terecht ongewijzigd. | Digest-gepinde Debian 13 distroless Node 22-runtime zonder shell of package manager, numeric non-root user, expliciete Sharp/libvips-runtime, absolute Node-healthchecks en een repositorycontracttest. Het volledige kandidaatimage is lokaal met Trivy 0.70.0 op 0 HIGH/0 CRITICAL gescand en functioneel gerooktest. | Closed in code; hosted immutable-image scan en stagingdeploy pending |
| R-065 | De PostgREST-deploychecker gebruikte voor één RPC een niet-bestaande named argument-key en verving lange veilige foutcodes door `REQUEST_FAILED`; de laatste recovery-RPC volgde na de laatste expliciete cache-reload. | Iedere correcte Phase-B-database-upgrade blokkeerde vóór appactivatie zonder bruikbare diagnose, terwijl een stale recovery-RPC pas tijdens wachtwoordherstel zichtbaar kon worden. | Gebruik exact `p_excluded_item_ids`, test de volledige probe tegen een lokale HTTP-contractserver, classificeer alleen vaste transport-/contractcodes, voeg een gegevensvrije forward-only schema-reload toe en probe `revoke_all_staff_app_sessions_for_user(null)`. | Closed in code; hosted deploybewijs pending |
| R-066 | De vorige stagingrelease `a846c059bce3d7e794504acca57a4771dfdb536d` gebruikt legacy QR-/uitgifte-RPC's waarvan execute-rechten in Phase B zijn ingetrokken. | Een automatische approllback na kandidaatactivatie zou een ogenschijnlijk draaiende maar operationeel onveilige oude app op het nieuwe schema starten. | Markeer exact dit stagingrollbackdoel als schema-incompatibel, eis vóór migratie dat app en scheduler gestopt zijn en stop bij kandidaatfalen fail-closed zonder de oude image te starten. Database-downmigratie blijft verboden; herstel is een forward-fix. | Closed in code; hosted deploybewijs pending |
| R-067 | De eerste publieke edgeprobe verwachtte dat een verkeerd content-type pas na volledige body-upload `415` zou geven. Caddy 2.10 wrapt de body echter lazy en de app wees ontbrekende Origin al vóór body-read met `403` af. Next bouwt achter Caddy bovendien een interne request-URL en zijn middlewareclone kapte standaard boven 10 MiB af. | Een correcte actieve Caddycap kon de immutable deploy vals blokkeren; de publieke origin vergelijken met de interne URL of de 12 MB-probe laten afkappen zou opnieuw vals rood geven. Simpelweg `403` accepteren zou juist iedere ontbrekende cap groen maken. | Behoud streaming en de harde `413`-eis, maar gebruik op de vier bestaande paden een side-effectvrije HMAC-drain. Verifieer publieke Host/X-Forwarded-Host/-Proto tegen `APP_BASE_URL`, gebruik de interne URL alleen voor pad/query en zet de middlewareclone exact op `12000001`. Exact max moet app-`204`+marker zijn; max+1 moet edge-`413` zonder marker zijn. Digest-gepinde Caddy- en echte Next-netwerktests bewijzen alle matchers, de 12 MB-doorgifte, vroege-403-regressie en detectie van capdrift. | Closed in code; exact-SHA stagingbewijs pending |
| R-069 | De gedeelde staging-Caddy handhaafde de repositoryreferentie voor requestbodycaps niet; `128001` chunked bytes bereikte de no-op appbranch met `204`. | Zonder een werkelijk actieve pre-parsergrens kan een aanvaller Nextmiddleware en de routeparser onnodig met oversized bodies belasten; een stil aangenomen hostconfig maakt releases bovendien niet reproduceerbaar. | Start in exact hetzelfde distroless app-image een minimale loopbackgateway die volledig buffert vóór doorsturen, CL+TE en hop-by-hopheaders fail-closed behandelt en per routegroep aparte capaciteit/tijd afdwingt. Eén vooraf begrensde buffer per request houdt de gezamenlijke maximale gatewaybuffer op `37632000` bytes; een trage import kan standaard-API, mail of webhook niet verdringen. Next behoudt de fijnmazigere parsergrens; de publieke deploygate eist nog steeds exact max `204` plus marker en max+1 markerloos `413`. | Closed in code; immutable-image en exact-SHA stagingbewijs pending |
| R-070 | `actions/upload-artifact@v4.6.2` gaf de resultaatartifactdigest als kale 64-hex SHA-256 terug, terwijl het stagingattest alleen `sha256:<hex>` accepteerde. | Een inhoudelijk volledig groene stagingdeploy eindigt zonder verifieerbaar rungebonden attest; daardoor blokkeren cleanup, acceptatie en promotie terecht ondanks een gezond artifact. | Normaliseer uitsluitend dit vertrouwde digestveld centraal naar `sha256:<hex>` en blijf algoritme, lengte en lowercase-hex strikt valideren. Creatie en verificatie delen dezelfde canonieke functie; een gerichte regressietest dekt zowel kale GitHub-uitvoer als de API-vorm met prefix. | Closed in code; exact-SHA deployattest pending |
| R-071 | De cleanupjobs vertrouwden impliciet op een globale Node-installatie op de self-hosted stagingrunner. | Een correct geïsoleerde runner stopt de dry-run met `node: command not found`; apply zou om dezelfde reden vóór backup en mutatie blokkeren. | Bootstrap Node 22 expliciet met dezelfde commit-gepinde setup-action in zowel dry-run als apply, na boundarybewijs en vóór ieder Node-entrypoint. Een workflowcontracttest bewaakt aantal, versie en volgorde. | Closed in code; hosted dry-run pending |
| R-072 | De bestaande stagingsecret `SUPABASE_DB_URL` bevat geen expliciete `sslmode`; libpq-queryparameters kunnen daarnaast de zichtbare URL-host of TLS via een Unix-socket overschrijven. | Cleanup blokkeert terecht vóór databaseconnectie; het versoepelen van de validator of alleen toevoegen van `sslmode=require` kan alsnog opportunistische TLS of een ander doel toelaten. | Normaliseer de secret eenmalig per cleanupjob via Node en GitHub `GITHUB_ENV`, maskeer de afgeleide URL en voeg alleen bij afwezigheid `sslmode=require` toe. Sta uitsluitend `sslmode` en begrensde `connect_timeout` toe; normalisator en targetvalidator weigeren alle doel-/credential-/serviceoverrides en duplicaten. Geef validate, backup en apply uitsluitend de genormaliseerde waarde. | Closed in code; hosted dry-run pending |
| R-073 | De browseracceptatie wachtte na een geslaagde lokale database-reset maximaal 30 seconden op de echte Supabase Auth-API. | Variabele containerherstarttijd kon een verder volledig groene exact-SHA-gate als flaky laten falen voordat testfixtures werden aangemaakt. | Blijf de echte admin-Auth-API pollen, maar gebruik een harde deadline van 120 seconden en ga onmiddellijk verder bij succes. Een niet-gezonde Auth-service blijft de job blokkeren. | Closed in code; hosted rerun pending |
| R-074 | De restore-inventaris maakte `supabase_functions_admin` verplicht, terwijl Supabase deze rol alleen heeft wanneer de Functions/pg_net-capability aanwezig is. | Een geldige encrypted stagingbackup werd netwerkloos hersteld maar vóór artifactupload afgekeurd; onvoorwaardelijk aanmaken zou daarna een bron/restore-roldrift veroorzaken. | Vereis exact elf kernrollen, sta uitsluitend de ene optionele rol toe, valideer bron en rolobjecten volledig en spiegel pas daarna create, alter en membership-grant in cleanup én restore-drill. De complete inventarisdigest blijft exact en iedere onverwachte image-/roldrift stopt vóór mutatie. | Closed in code; hosted backup/apply evidence pending |
| R-075 | Hosted Supabase-baselines kunnen verschillen in het lidmaatschap `postgres → supabase_realtime_admin`, terwijl de rol voor dump-/ACL-restore wel aanwezig moet blijven. | Een hardcoded grant wijzigt de exacte rolleninventaris; een brede dynamische reconstructie kan onverwachte privilegepaden toevoegen. | Spiegel uitsluitend dit bekende optionele membership via een literal boolean uit de volledig gevalideerde bron. Kernmemberships blijven verplicht, overige memberships zijn gesloten en de volledige bron/restore-inventaris-SHA blijft exact. | Closed in code; hosted backup/apply evidence pending |
| R-076 | De Mail-v2-editor schakelde tijdens iedere async actie tussen editable en read-only met TipTap `setEditable` zonder de standaard ingeschakelde `emitUpdate` uit te zetten; content-sync luisterde alleen naar de revisiesleutel en kon de één render later gecommitte template-inhoud missen. | Na een geldige previewresponse veroorzaakte het terugschakelen naar editable een inhouds-`onUpdate`; de parent markeerde de ongewijzigde template daardoor dirty en wiste de zojuist gecommitte preview. Bij echte templatewisseling kon de editor daarnaast de vorige inhoud zichtbaar houden. | Gebruik voor uitsluitend de editable-sync expliciet `setEditable(..., false)`; echte gebruikerswijzigingen blijven via `onUpdate` lopen. Synchroniseer gewijzigde content eventloos wanneer zij structureel afwijkt van de actuele editor-JSON, zodat gewone invoer de selectie niet reset. jsdom-regressies bewijzen TipTaps standaardevent, de eventloze applicatieaanroep en de vertraagde statecommit tussen twee templates; de browsertest valideert één request, responsebody, previewstatus, modus, frametitel en exacte `srcdoc` zonder retry of skip. | Closed in code; exact-head rerun pending |
| R-077 | Het stagingacceptatieharnas initieerde een refund die volgens productbesluit uitsluitend extern wordt uitgevoerd en daardoor providerafhankelijk `pending` bleef. | Een buiten-scope handeling maakte de releasegate vals rood zonder de vereiste betaal-, mismatch-, replay- of readinessflow af te keuren. | Verwijder alleen refundinitiatie uit het harnas en promotiecontract. Behoud alle webhookreconciliatie en regressies voor extern waargenomen `processing/refunded`; de exacte betaal-, mismatch-, replay- en readinessacceptatie blijft verplicht. | Closed in code; exact-main hosted rerun pending |
| R-078 | De coreacceptatie navigeerde voor de uitgifterol direct na de MFA-sessieresponse zelf naar `/backoffice`, terwijl de loginpagina gelijktijdig al de canonieke redirect naar `/uitgifte` uitvoerde. | Twee Playwrightnavigaties konden elkaar onderbreken en een werkende MFA-/scannersessie als generieke stagingfout rapporteren. | Bewijs eerst de afgeronde automatische scannerlanding en sessie, start pas daarna de afzonderlijke backoffice-boundaryprobe en gebruik voor iedere fase een stabiele PII-vrije foutcode. | Closed in code; exact-main hosted rerun pending |
| R-079 | Voorraadtekortactiepunten gebruikten dezelfde variantgebonden dedupe-key maar namen het wisselende brontype van betaling, scheduler of levering over. | Een betaalde order kon na latere voorraadontvangst op `ACTION_ITEM_DEDUPE_COLLISION` stoppen, waardoor allocatie en QR-readiness transactioneel werden teruggedraaid. | Gebruik een stabiele `article_variant`-bron voor de projectie, normaliseer alle actieve legacyepisodes in een forward-only migratie en test een refresh via twee verschillende operationele bronnen tegen één actieve episode. | Closed in code; exact-main Mollieacceptatie pending |
| R-080 | GitHub Actions bewaart bij job-reruns gelijknamige manifest- en attestationartifacts uit eerdere pogingen, terwijl de releaseverifier over de hele run exact één naam eiste. | Een correct hergebruik van hetzelfde ondertekende artifact werd na een groene activatie alsnog als dubbel bewijs afgewezen; alle volgende acceptatiepoorten blokkeerden in de secrets-vrije preflight. | Bind iedere artifactselectie aan het tijdvenster van de actuele producerende job, eis daar exact één geldige identiteit en behoud de bestaande runattempt-/digest-/resultaathashbinding. Historische artifacts blijven beschikbaar maar tellen niet als actueel bewijs. | Closed in code; exact-main rerun- en promotiebewijs pending |
| R-081 | De PostgreSQL-snapshotcontainer maakte dump en broninventaris als root-owned mode `0600`, waardoor de hosted runner de inventaris na een geldige snapshot niet kon lezen. | Restorebewijs stopte vóór netwerkloos herstel en artifactupload, ondanks een correcte databasebackup. | Maak beide bind-mountdoelbestanden vooraf als runner-owned mode `0600`; container-root truncateert de bestaande inode zonder ownership te wijzigen. Contracttest bewaakt initialisatie vóór de snapshotcontainer. | Closed in code; hosted restore rerun pending |
| R-082 | De Mollie-fixturecleanup kon botsen met de autonome mailscheduler nadat de betaalwebhook al een immutable delivery-attempt en actiepunt had aangemaakt. | De fixture bleef achter en een volgende acceptance kon blokkeren, terwijl generiek verwijderen van immutable mailbewijs onveilig is. | Verzamel eerst exact de rungebonden job-/attempt-ID's na volledige fixtureownership-preflight, verwijder alleen hun eventledger transactioneel met tijdelijk uitgeschakelde immutable triggers, herstel triggers vóór commit en verifieer mail-, actiepunt-, order- en ouderstaat leeg. De echte SQL-integratietest claimt nu een delivery-attempt en maakt een actiepunt vóór cleanup. | Closed in code; exact-main hosted acceptance pending |
| R-083 | SendGrid rapporteert voor de herhaaldelijk in het dashboard gecontroleerde staging-runtimekey aanvullende scopes naast `mail.send`. | De releaseketen kan provider-least-privilege niet betrouwbaar machinebewijzen; misbruik van eventuele werkelijke extra rechten blijft een extern configuratierisico. | Op expliciet besluit van de producteigenaar blokkeert alleen ontbrekend `mail.send`. Key- en accountfingerprints, vaste afzender, app-acceptatie, replayhergebruik, recipient-server `delivered`-event, cleanup en post-delivery health blijven fail-closed. De scopeworkflow is read-only en muteert geen key. | Accepted external provider-configuration risk; exact signed recipient-server evidence pending |
| R-084 | De eenmalige legacy-adoptie en twee latere rollbacktargetsynchronisaties vertrouwden op een globale Node-installatie op geïsoleerde self-hosted runners. | De read-only capture stopte veilig vóór `docker save`; zonder gezamenlijke correctie zouden stagingadoptie of productiepromotie later op dezelfde ontbrekende runtime falen. | Installeer na iedere runnerboundary en vóór ieder Node-afhankelijk script dezelfde commit-gepinde Node 22-setup. Workflowtests bewaken aanwezigheid, versie en volgorde voor capture, adoptie, kandidaatcapture en duurzame synchronisatie. | Closed in code; exact-main hosted adoption pending |
| R-085 | De publieke legacyproductie is gezond, maar de geïsoleerde productionrunner ziet geen draaiende appcontainer in zijn eigen Rootless-Dockerdaemon. | Een lokale tag en user-owned manifest delen één mutable trustdomain en leveren daarom zwakkere provenance dan een zichtbare live-containerbinding. | De producteigenaar accepteert voor exact deze eenmalige overgang een zichtbaar benoemde fallback. Lock-first, exact nul containers, mode/owner, daemon, revision, manifest/tag/config/alle layers/releaselabel, publieke én loopbackhealth en bytegelijke state vóór/na zijn verplicht; signed bewijs zet `live_container_bound=false`. Rebuild/pull/load/start blijft verboden en staging moet kandidaat→legacy→kandidaat bewijzen. | Accepted one-time provenance exception; hosted bytecapture/adoption pending |
| R-086 | De SendGrid-acceptatie eiste `enabled=true` in het antwoord van de signed-public-key-GET, terwijl het officiële endpoint alleen `id` en `public_key` retourneert. | Een correct geconfigureerde signed webhook werd vóór iedere testmail vals rood afgekeurd. | Gebruik PATCH uitsluitend om signing in de staging-configuratieworkflow in te schakelen; bewijs in de read-only acceptance-GET exact webhook-ID, geldige P-256-key en fingerprintbinding. Een regressietest gebruikt bewust geen `enabled`-responseveld. | Closed in code; exact-main inbox/event evidence pending |
| R-087 | De Node-gebaseerde SendGrid-acceptatie synchroniseerde een AAL2-sessie zonder de fetch-metadata- en CSRF-headers die de browsermutatieguard verplicht stelt. | De tijdelijke beheerder bereikte AAL2, maar de app wees de sessie-POST terecht met HTTP 403 af vóór iedere testmail. | Laat de harness exact het browsercontract sturen: gelijk `Origin`, `Sec-Fetch-Site: same-origin` en `X-Duindorp-CSRF: same-origin`. Versoepel de serverguard niet en bewijs de headers in de providerregressietest. | Closed in code; exact-main inbox/event evidence pending |
| R-088 | Na de gerepareerde AAL2-sessie miste de testdelivery-POST hetzelfde browsermutatiecontract; cleanup logde lokaal uit vóór globale admin-intrekking met dezelfde JWT. | Mail Send werd opnieuw vóór verzending geblokkeerd en de al ongeldig gemaakte JWT veroorzaakte een tweede cleanupfout, waardoor alleen een gecombineerde foutcode zichtbaar was. | Stuur op alle appmutaties gelijk Origin, fetch-metadata en CSRF; trek de tijdelijke sessie eerst globaal in via de adminclient en verwijder daarna de gemarkeerde fictieve gebruiker. De regressie bewaakt beide deliveryaanvragen en de intrekkingsvolgorde. | Closed in code; exact-main inbox/event evidence pending |
| R-089 | Het SendGrid-harnas eiste dat `mail_templates_v2` operationeel actief was voordat de beheerderstestmail het providerpad mocht bewijzen. | Provideracceptatie en mailcutover waren circulair afhankelijk; een veilig gepubliceerde testtemplate kon vóór operationele activering nooit tot Mail Send komen. De samengestelde foutcode onderscheidde bovendien query-, template- en brandingstate niet. | Volg het bestaande testdeliverycontract: vereis gepubliceerde template/branding en runtimeprovider, maar niet de operationele databaseflag. Houd ledenevents/queue buiten het testpad en rapporteer drie afzonderlijke PII-vrije readinesscodes. | Closed in code; exact-main inbox/event evidence pending |
| R-090 | De variantvalidator behandelde een alias die gelijk was aan het eigen maatlabel of de eigen code als een botsing en vertaalde die naar `VARIANT_MATCH_KEY_EXISTS`. | Een beheerder kon op staging geen eerste variant opslaan wanneer een redundante importalias was ingevuld; de melding suggereerde ten onrechte bestaande catalogusdata. | Filter redundante eigen aliassen identiek aan de database-normalisatie, bewaar alleen aanvullende aliassen en behoud duplicate-/cross-variantconstraints en de productsleutellock. | Closed in code; exact-main stagingdeploy pending |
| R-091 | Ontbrekende optionele Sportlinkvelden of onbekende maten maakten een hele rij operationeel onbruikbaar, terwijl handmatige uitzonderingen alleen buiten de gecontroleerde importflow konden worden verwerkt. | Geldige leden konden niet worden geïmporteerd; ad-hoc databasecorrecties zouden identiteit, audit en seizoensgrenzen kunnen omzeilen. | Per immutable mappingrevisie een standaard-uitgeschakelde, begrensde negeermodus; filter vóór duurzame staging, harde identiteits-/DOB-/maatbescherming, nullable team met expliciete `legacy_unknown`-reconciliatie, en een AAL2-admin-RPC voor idempotente handmatige invoer met duplicatepreflight. | Closed in code; exact-main stagingacceptatie pending |
| R-092 | Bulktoewijzing kon de pakketinhoud los van de door het lid bevestigde maat vastleggen of de zichtbare paginaselectie verwarren met alle actieve leden. | Verkeerde maten, ontbrekende leden en niet-reproduceerbare vraag/voorraad zouden ontstaan. | Leid alle actieve lid-seizoenen server-side af; bind preview aan databaserevisie; materialiseer uitsluitend actieve `confirmed`/`locked` varianten en rapporteer iedere ontbrekende maat zonder te gokken. | Closed in code; featurebranch is niet gedeployed |
| R-093 | Hard verwijderen of leegmaken van een pakketorder zou commerciële snapshots en logistieke historie breken; onbegrensd intrekken na betaling of reservering zou geld- en voorraadverschillen veroorzaken. | Audit, ouderhistorie, reconciliatie en voorraad kunnen onherstelbaar uiteenlopen. | Gebruik een expliciete soft-withdrawalstatus, bewaar revisie/prijs/snapshots en ingetrokken regels, sta intrekken alleen vóór financiële/logistieke voortgang toe en blokkeer downstream writes met databaseguards. | Closed in code; featurebranch is niet gedeployed |
| R-094 | Betaling, maatcampagnes en betaalbevestiging vereisten impliciet minimaal één gematerialiseerde orderregel, terwijl pakketregels pas na maatbevestiging ontstaan. | Een toegewezen pakket zonder bevestigde maten kon niet betrouwbaar betalen of in het juiste invul-/controlemailsegment komen; een betaalwebhook kon door een mislukte mailproducer transactioneel worden teruggedraaid. | Laat commerciële pakketprocessen uit de actieve immutable pakketsnapshot werken, behoud logistieke processen op regels, geef invullen voorrang boven controleren en bewijs nul-regelbetaling, payload, campagne, eventstate, legacycompatibiliteit en concurrency. | Closed in code; featurebranch is niet gedeployed |
| R-095 | De centrale browsermutatieallowlist kende `PUT` niet, terwijl leveringconcept en lagevoorraaddrempel via beveiligde PUT-routes worden opgeslagen; route-unittests mockten de guard. | Geldige beheerdersacties werden vóór autorisatie en database met HTTP 405 afgewezen en toonden alleen de generieke veilige-controlefout. | Voeg PUT aan dezelfde gesloten mutatieguard toe, behoud alle overige requestchecks en test zowel de validator als de echte guard met geldige same-origin en geweigerde cross-origin PUT. | Closed in code; featurebranch is niet gedeployed |
| R-096 | De FIFO-UI leidde productinformatie af uit losse actieve varianten en toonde iedere pakketregel als een afzonderlijke naamrij. | Eén lid verscheen meerdere keren en historische/inactieve regels konden hun herkenbare productcontext verliezen. | Groepeer uitsluitend in de UI op order-ID, behoud regelvolgorde/status en projecteer immutable product-/maatsnapshots via een AAL2-beveiligde forward-only workspace-RPC. | Closed in code; featurebranch is niet gedeployed |
| R-097 | De immutable requestledger voor pakketbulkacties stond niet in de gesloten staging-cleanupinventaris. | De CI-cleanupgate blokkeerde terecht en een latere operationele stagingreset zou anders request-/resultaatsnapshots achterlaten. | Voeg uitsluitend `private.member_package_bulk_requests` aan de operationele allowlist toe, verhoog de exacte inventaristelling en bewijs rollback, commit, lege postconditie en behoud van staff/Auth/config met de echte SQL-test. | Closed in code; hosted rerun pending |
| R-098 | Een brede ledeneditor kon externe Sportlink-identiteit, oudergrants, historische seizoenen of uitgegeven/gereserveerde maten stil overschrijven; nieuwe requestledgers konden buiten de gesloten stagingcleanupinventaris vallen. | Gezinnen konden toegang verliezen, herimport kon dubbelen maken, geld-/voorraadhistorie kon afwijken of de stagingcleanup kon pas na merge blokkeren. | Houd relatienummer en portaaltoegang buiten de generieke editor; wijzig alleen het actieve lid-seizoen; vereis beheerder+AAL2 voor identiteit; gebruik revisie, idempotentie, lidlocks en PII-vrije audit; laat reserveringen alleen expliciet journal-aware vrijgeven; neem beide ledgers op in de exacte 105-wis/28-preserve/133-totaalinventaris. | Closed in code; exact-SHA stagingacceptatie pending |
| R-099 | De appcontainer gebruikte volledige operationele readiness als Docker-healthcheck, terwijl de scheduler pas na een gezonde app mocht starten. | Een verouderde herstelbare operationele marker veroorzaakte een startdeadlock: app en vorige rollbackimage bleven `unhealthy`, ook al draaide Next.js normaal; de scheduler kon de marker niet bijwerken. | Gebruik een PII-vrije proceslivenessroute voor Compose, behoud volledige readiness als latere harde deploygate en wacht begrensd maximaal vijf minuten op de eerste schedulercyclus. | Closed in code; exact-main stagingredeploy pending |
| R-100 | Twee historische OTP-`configuration_error`-uitkomsten bleven 24 uur als actueel providerincident tellen, terwijl de actuele runtime/providerbinding afzonderlijk werd gevalideerd; de nieuwe Composehealthcheck maakte bovendien de vorige image onbruikbaar voor rollback omdat die `/api/live` nog niet bevatte. | Staging bleef na een gezonde schedulercyclus 503 en de bewezen vorige applicatie kon onder het nieuwe livenesscontract niet automatisch terugstarten. | Health v13 bewaart de immutable incidenten maar laat actuele configuratie uitsluitend door de bestaande runtime-/database-/sleutelpoorten bepalen; echte provider-/render-/deliveryfouten blijven blokkeren. De rollbackhealth valt uitsluitend bij een ontbrekende `/api/live`-route (`404`) terug op de oude hoofdpagina. | Closed in code; exact-main stagingredeploy pending |
| R-101 | Technische oudercopy noemde interne begrippen en de OTP-systeemfallback week af van de goedgekeurde ledencommunicatie. | Ouders konden de maat-, betaal- en afhaalflow verkeerd begrijpen; een brede templatemigratie zou tegelijk bewust gepubliceerde mailinhoud kunnen overschrijven. | Pas uitsluitend de aangeleverde zichtbare afwijkingen en onaangeraakte systeemdefaults aan, behoud bestaande veiligheidschecks en laat medewerkersinhoud ongemoeid. Gerichte tests bewaken kritieke copy en de verwijderde interne Mollie-disclaimer. | Closed in code; exact-main stagingbewijs pending |
| R-102 | Het productie-recoveryharnas eiste stil dat ook de geadopteerde legacybron PostgreSQL major 17 draaide, terwijl alleen het geïsoleerde hersteldoel hard op 17 hoeft te staan. | Een geldige oudere Supabasebron stopte vóór versleuteling, restorebewijs en iedere productiemutatie; de ontbrekende fasemelding maakte de veilige stop niet diagnostisch. | Sta uitsluitend in legacy-`source`-modus bronmajor 15–17 toe, houd stagingbron en restorecontainer exact op 17, vergelijk alle overige inventarissecties exact, publiceer bron-/doelmajor apart in evidence schema v5 en log bij snapshotfalen alleen een vaste veilige fasenaam. | Closed in code; exact-main restore/promotion evidence pending |
| R-103 | De actuele Distroless Debian 13-runtime bevat `CVE-2026-14456` in `libssl3t64`; upstream levert nog geen fixed package of nieuwere digest. | De onveranderde HIGH/CRITICAL-releasegate blokkeert een urgente applicatiehotfix, terwijl een brede uitzondering toekomstige kwetsbaarheden onzichtbaar zou maken. | Accepteer tijdelijk uitsluitend de exacte CVE+package-PURL tot 2026-09-01 omdat de runtime geen OpenSSL-QUIC gebruikt; behoud digestpinning, `ignore-unfixed: false`, SBOM en de harde poort voor iedere andere bevinding. | Accepted tijdelijk; verwijderen bij gefixte Distroless-digest of uiterlijk 2026-09-01 |
| R-104 | Een direct geladen supportwidget kan derde-partijopslag activeren en gevoelige portaalcontext onbedoeld met een leverancier delen; een globale CSP-verruiming vergroot bovendien het aanvalsoppervlak van alle applicatieoppervlakken. | Privacyverlies, ongewenste tracking of zwakkere browserisolatie op staff-, uitgifte- en betaalroutes. | Laad LiveChat uitsluitend na expliciete ouderactie, geef geen applicatie-identifiers of persoonsgegevens door, groepeer alleen de publieke ouderroutes onder de widgetlayout en pas de minimale leveranciers-CSP uitsluitend daar toe. | Closed in code; hosted browseracceptatie pending |
| R-105 | SQLSTATE `40001` werd gebruikt voor duurzame business-stateconflicten in plaats van alleen echte serialisatiefouten. | De database-/API-laag herhaalde dezelfde calls in een tight loop: miljoenen rollbacks, database-uitval, 504's en mislukte OTP-aanvragen. | Retourneer pending/absent als getypeerde data, behoud gecontroleerde HTTP-retry voor SendGrid, leg de directe stagingcontainment vast als forward migration en test readiness, mail-testrace en dubbele finalisatie zonder `40001`. | Closed on staging and in code; exact-SHA deployment pending |
- **SMTP stagingacceptatie nog extern geblokkeerd (2026-08-19):** de repository bevat geen stagingcredentials of geauthenticeerde deploymenttoegang. Houd `EMAIL_BULK_ENABLED=false` totdat SMTP verify, beheer-testmail, OTP en individuele uitnodiging aantoonbaar zijn geaccepteerd; SMTP-acceptatie is geen inbox-deliverybewijs.

| R-106 | Globale maatregels per lid, seizoen en artikel konden bij pakketwissels als actuele keuze worden hergebruikt en globale cataloguspakketten waren door ouders selecteerbaar. | Een veldspeler kon keeperproducten zien of een nieuwe assignment kon een oude bevestiging als actuele pakketkeuze erven. | Gebruik de immutable pakketsnapshot als assignment-ID, projecteer iedere bevestiging assignmentgebonden, maak ouderkeuze onmogelijk, filter backoffice op de actieve snapshot en laat reservering pas bij zowel geldige maat als betaling ontstaan. | Closed in code; lokale databaseacceptatie vereist Docker |
| R-107 | De staging-scheduler logde bij private health-HTTP 503 alleen de generieke statuscode. | Een fail-closed release kon niet gericht worden hersteld zonder tijdelijk bredere stagingtoegang of herhaalde blinde deploypogingen. | Log uitsluitend een vaste allowlist van operationele status-, boolean- en telvelden; onbekende velden, secrets en persoonsgegevens worden nooit overgenomen. Een unitregressie bewijst zowel de diagnose als de uitsluiting. | Closed in code; hosted diagnose pending |
| R-108 | Releasehealth telde iedere `failed` e-mailjob als operationeel incident, ook wanneer een geautoriseerde pre-send controle verzending bewust stopte zonder providerpoging. | Ingetrokken toegang of gewijzigde eligibility kon een verder gezonde stagingrelease permanent blokkeren en ook rollbackhealth rood houden. | Behoud de immutable jobhistorie, maar sluit uitsluitend vijf expliciet benoemde veilige terminalredenen uit. Neem de oudere `access_revoked_before_send`-reden ook op in de interne incidentfilter. Nieuwe echte queue-, provider-, render- en deliveryfouten blijven fail-closed; pgTAP bewijst beide kanten. | Closed in code; exact-main stagingredeploy pending |
| R-109 | Eén echte mailfailure van vóór de Phase B-release bleef na correcte classificatie als operationeel werk bestaan en blokkeerde daardoor iedere nieuwe appimage én de scheduler van de rollbackimage. | Een historisch incident kan een ongerelateerde forward-only release permanent deadlocken, terwijl een brede tijdsgrens andere failed, uncertain of providerincidenten onzichtbaar zou maken. | Valideer dat hoogstens één kandidaat bestaat en leg uitsluitend diens job-ID plus exacte `updated_at` vast; behoud job, afleverhistorie en actiepunt. Iedere andere job, uncertainty en providerfailure blijft blokkeren, net als een latere failureversie van dezelfde job. | Closed in code; exact-main stagingredeploy pending |
| R-110 | Retentionhealth markeerde iedere oude `running`-ledger permanent stale, ook wanneer een latere retentionrun aantoonbaar succesvol was afgerond. | Eén historische procesonderbreking hield zowel kandidaat- als rollbackscheduler rood ondanks actuele succesvolle uitvoering. | Bewaar alle runledgerfeiten, maar tel alleen een oude running-run zonder later gestarte én `succeeded` geëindigde run van dezelfde operation. Een nieuwere of alleen overlappende oudere run die vijftien minuten blijft hangen blijft fail-closed; pgTAP bewijst herstel én blokkade. | Closed in code; exact-main stagingredeploy pending |

## Pakketlifecycle-reconciliatie — 2026-08-19

- Live/staging aantallen worden bewust niet in code verankerd. De forward-only reconciliatie detecteert paid actieve pakketten met ontbrekende componentregels op invariant en legt de werkelijke aantallen per categorie vast.
- Provider-paid blijft leidend bij ontbrekende maten; dit voorkomt betalingsverlies maar laat een expliciete operationele `Nalevering` staan totdat geldige varianten zijn gekozen.
- De reconciliatie schrijft `failed` zodra een betaald pakket niet veilig kan worden gematerialiseerd. Daarmee blijft de providerbetaling intact, terwijl rolloutacceptatie expliciete operationele beoordeling vereist.

## Follow-up databasecompatibiliteit — 2026-08-20

- De eerste lifecycle-migratie kon een reeds bestaande maar nog niet gelinkte pakketregel als losse regel zien. De follow-up adopteert alleen een unieke, exact passende componentregel; duplicaten, geannuleerde links en variant-/quantityafwijkingen worden nooit automatisch herschreven.
- De legacy-upgradefixture reproduceerde een PL/pgSQL-naambotsing tussen een `record`-variabele en SQL-alias. De lifecyclefunctie gebruikt nu onderscheidende namen en wordt zowel via een schone reset als de Phase-B-upgradegate gevalideerd.

| R-111 | Een gewone profielwijziging kon `app.members.email` los wijzigen van het autoritatieve ouderaccount en de actuele portalgrants. | Het nieuwe adres kon geen OTP krijgen, het oude adres behield toegang en meerdere kinderen van hetzelfde ouderaccount konden verschillende contactidentiteiten tonen. | Leid de actieve familie uitsluitend af uit echte grants, vereis een gezinsbrede revisiepreflight, wijzig member-e-mails en grantoverdracht atomair, serialiseer met activatie/revocatie/import/OTP/sessies en hergebruik de bestaande Mail-v2-activatieproducent. | Closed in code; exact-main CI en stagingacceptatie pending |
| R-112 | Een bestaande accountloze open doelgrant werd bij een gezins-e-mailtransfer overgeslagen wanneer het doelaccount al bestond. | De daaropvolgende insert botste op `parent_portal_grants_open_email_idx`, waardoor de beheerder na een geldige preflight alleen een generieke opslagfout zag; een nieuwe mailstopreden en onvoltooide batch konden bovendien de operationele health vertekenen. | Hergebruik de unieke pending/review-grant ook wanneer haar account nog null is, bind haar atomair aan het doelaccount, voltooi de batch en gebruik de reeds geclassificeerde veilige revocatiereden. Houd een resterende `23505` als expliciete 409 en log onbekende fouten PII-vrij. | Closed in code; exact-main CI en stagingacceptatie pending |
| R-113 | Een heraanvraag verving de actieve OTP direct, terwijl Gmail meerdere berichten in één gesprek kon groeperen. | Een ouder kopieert een oudere, zojuist ongeldig gemaakte code en raakt in een foutlus; extra aanvragen vergroten rate-limit- en supportdruk. | Eén actieve tienminutenuitdaging levert deterministisch dezelfde code en linkproof; resend verlengt niets, vijf fouten blokkeren beide methoden en een nieuwe challenge is expliciet. Plaintext credentials worden nergens duurzaam opgeslagen. | Closed in code en lokale concurrency; exact-SHA stagingbewijs pending |
| R-114 | Een directe bearerlink kan via querystrings, referrers, analytics, logs, replay of staffresponses lekken. | Diefstal geeft dezelfde gezinstoegang als een geldige OTP en kan meerdere gegrante kinderen raken. | Gebruik uitsluitend een URL-fragment, verwijder dit vóór netwerk- of assetverkeer, POST same-origin met no-store/gesloten referrerbeleid en consumeer exact dezelfde challenge atomair. Geen derde-partijasset op `/login/direct`; support ziet nooit de proof. | Closed in code, tests en threat model; hosted browserbewijs pending |
| R-115 | SMTP-acceptatie werd als aflevering gepresenteerd en individuele recipient failures konden globale readiness blokkeren. | Staff trekt een onjuiste deliverabilityconclusie of één fout adres houdt staging/applicatie 503, terwijl bulkmail een bewezen onbereikbaar adres blijft raken. | Projecteer provideracceptatie, bewezen aflevering, onzekerheid en permanente recipient failure afzonderlijk; aggregeer recipienthealth/suppressie per historisch genormaliseerd adres; degradeer globale health alleen voor systemische fouten. | Closed in code en lokale provider-/healthtests; hosted bewijs pending |
| R-116 | Een staging-OTP-test kan een echt lid mailen of credentials, sessies en persoonsgegevens in Actions-output/artifacts bewaren. | Ongeautoriseerde toegang, privacyverlies en onbruikbaar releasebewijs. | Exact-main/deploy-SHA-preflight, expliciete staginggate, uitsluitend de geconfigureerde veilige ontvanger, run-unieke zelfopruimende fixtures, always-cleanup en een gesloten PII-/credentialvrij evidence-schema. De workflow kan production niet targeten of promoten. | Workflow- en harnascontract lokaal groen; hosted bewijs pending |
| R-117 | De recipienthealthmigratie moest historische OTP-afleverpogingen aan een adresidentity binden, maar de bestaande append-only trigger blokkeerde iedere update. Een later ingevoegde brugmigration kan bovendien ouder zijn dan de remote migratiekop. | De stagingdeploy stopt midden in de migratieketen; eerdere migraties zijn al vooruitgerold terwijl appvervanging terecht uitblijft. Een remote die de backfill bij een lege ledger al passeerde kan een oudere pending migration anders weigeren. | Open vóór de backfill een forward-only guardvenster dat uitsluitend `recipient_identity_id: NULL -> UUID` toestaat wanneer alle overige kolommen identiek blijven en zichzelf op de backfillreconciliatie sluit; herstel daarna de oorspronkelijke guard. Gebruik gevalideerde `--include-all` dry-run/apply en bewijs een niet-lege upgradefixture. | Closed in code en lokale upgradegate; exact-main redeploy pending |
